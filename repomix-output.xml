This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
public/
  favicon.ico
  favicon.svg
src/
  components/
    islands/
      AdminDashboard.tsx
      CartBadge.tsx
      CartCheckout.tsx
      CategoriesGrid.tsx
      EmployeePortal.tsx
      GoogleReviewLink.tsx
      LoyaltyDashboard.tsx
      MayoreoForm.tsx
      MenuCatalog.tsx
      PlanCard.tsx
      PlansList.tsx
      POSIsland.tsx
      PromotionsBoard.tsx
      QrPreview.tsx
      StaffLogin.tsx
      StarRating.tsx
      SurveyForm.tsx
    Footer.astro
    Navbar.astro
  data/
    inventory.ts
    plans.ts
    products.ts
    promotions.ts
    site.ts
    team.ts
    wholesale.ts
  hooks/
    useMounted.ts
    useStaffSession.ts
    useSupabaseData.ts
  layouts/
    AdminLayout.astro
    Layout.astro
  lib/
    queries.ts
    subscriptionDiscount.ts
    supabase.ts
    utils.ts
  pages/
    api/
      checkout/
        create-session.ts
    carrito/
      exito.astro
    admin.astro
    carrito.astro
    empleados.astro
    encuestas.astro
    fidelizacion.astro
    index.astro
    mayoreo.astro
    menu.astro
    pos.astro
    promociones.astro
    suscripciones.astro
  stores/
    cart.ts
    loyalty.ts
  styles/
    global.css
  env.d.ts
.gitignore
AGENTS.md
astro.config.mjs
package.json
playwright.config.ts
pnpm-workspace.yaml
README.md
requerimientos-don-zanahorio.md
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/components/islands/AdminDashboard.tsx">
import { useEffect, useState } from "react";
import {
  LayoutDashboard,
  ShoppingCart,
  Package,
  ClipboardList,
  Users,
  UserPlus,
  Ticket,
  Star,
  LogOut,
  Menu,
  X,
  Plus,
  UtensilsCrossed,
} from "lucide-react";
import { useStaffSession } from "@/hooks/useStaffSession";
import StaffLogin from "@/components/islands/StaffLogin";
import { formatMXN, cn } from "@/lib/utils";
import { useSupabaseData } from "@/hooks/useSupabaseData";
import {
  fetchAdminOrders,
  updateOrderStatus,
  fetchAdminSurveys,
  fetchAdminWholesaleOrders,
  updateWholesaleStatus,
  fetchAdminJobApplications,
  updateJobApplicationStatus,
  fetchAdminReferrals,
  updateReferralStatus,
  markReferralHired,
  claimReferralBonus,
  type AdminReferral,
  fetchAdminGoogleReviewsCount,
  fetchAdminCustomers,
  fetchAdminCoupons,
  createCoupon,
  toggleCouponActive,
  fetchAdminProducts,
  updateProductPricing,
  type AdminOrder,
} from "@/lib/queries";

type Section =
  | "resumen"
  | "pedidos"
  | "mayoreo"
  | "encuestas"
  | "clientes"
  | "empleo"
  | "referidos"
  | "cupones"
  | "productos"
  | "resenas";

const NAV_GROUPS: { label: string; items: { id: Section; label: string; icon: typeof LayoutDashboard }[] }[] = [
  { label: "General", items: [{ id: "resumen", label: "Resumen", icon: LayoutDashboard }] },
  {
    label: "Operación",
    items: [
      { id: "pedidos", label: "Pedidos", icon: ShoppingCart },
      { id: "mayoreo", label: "Mayoreo", icon: Package },
    ],
  },
  {
    label: "Clientes",
    items: [
      { id: "clientes", label: "Fidelización", icon: Users },
      { id: "encuestas", label: "Encuestas", icon: ClipboardList },
      { id: "resenas", label: "Reseñas Google", icon: Star },
    ],
  },
  {
    label: "Personal",
    items: [
      { id: "empleo", label: "Solicitudes de empleo", icon: UserPlus },
      { id: "referidos", label: "Referidos", icon: UserPlus },
    ],
  },
  {
    label: "Marketing",
    items: [
      { id: "cupones", label: "Cupones", icon: Ticket },
      { id: "productos", label: "Menú y precios", icon: UtensilsCrossed },
    ],
  },
];

const SECTION_LABELS: Record<Section, string> = {
  resumen: "Resumen",
  pedidos: "Pedidos",
  mayoreo: "Pedidos de mayoreo",
  encuestas: "Encuestas",
  clientes: "Clientes de fidelización",
  empleo: "Solicitudes de empleo",
  referidos: "Referidos de empleados",
  cupones: "Cupones",
  productos: "Menú y precios",
  resenas: "Reseñas de Google",
};

function Badge({ children, tone = "neutral" }: { children: React.ReactNode; tone?: "neutral" | "good" | "warn" | "bad" }) {
  const tones: Record<string, string> = {
    neutral: "border-carrot-50/15 bg-carrot-50/10 text-carrot-50/70",
    good: "border-leaf-700/40 bg-leaf-500/15 text-leaf-300",
    warn: "border-carrot-700/40 bg-carrot-500/15 text-carrot-300",
    bad: "border-red-800/40 bg-red-500/15 text-red-300",
  };
  return (
    <span className={cn("inline-flex items-center rounded-full border px-2 py-0.5 text-[11px] font-medium capitalize", tones[tone])}>
      {children}
    </span>
  );
}

function StatCard({ label, value, hint }: { label: string; value: string | number; hint?: string }) {
  return (
    <div className="glass glass-card rounded-2xl p-5">
      <p className="text-xs font-medium text-carrot-50/60">{label}</p>
      <p className="mt-2 text-2xl font-bold text-carrot-50">{value}</p>
      {hint && <p className="mt-1 text-xs text-carrot-50/50">{hint}</p>}
    </div>
  );
}

function Table({ head, children }: { head: string[]; children: React.ReactNode }) {
  return (
    <div className="glass overflow-x-auto rounded-2xl">
      <table className="w-full text-sm">
        <thead className="border-b border-carrot-50/10 text-left text-[11px] uppercase tracking-wide text-carrot-50/50">
          <tr>
            {head.map((h) => (
              <th key={h} className="px-4 py-3 font-medium">
                {h}
              </th>
            ))}
          </tr>
        </thead>
        <tbody className="divide-y divide-carrot-50/10">{children}</tbody>
      </table>
    </div>
  );
}

function StatusSelect({
  value,
  options,
  onChange,
}: {
  value: string;
  options: string[];
  onChange: (v: string) => void;
}) {
  return (
    <select
      value={value}
      onChange={(e) => onChange(e.target.value)}
      className="rounded-full border border-carrot-50/15 bg-ink-900 px-2 py-1 text-xs text-carrot-50 capitalize focus:outline-none focus:ring-1 focus:ring-carrot-400"
    >
      {options.map((o) => (
        <option key={o} value={o} className="bg-ink-900">
          {o}
        </option>
      ))}
    </select>
  );
}

function OrdersSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminOrders);
  const [rows, setRows] = useState<AdminOrder[] | null>(null);
  useEffect(() => setRows(data), [data]);

  async function changeStatus(id: string, status: string) {
    setRows((prev) => prev?.map((o) => (o.id === id ? { ...o, status } : o)) ?? null);
    await updateOrderStatus(id, status).catch(() => {});
  }

  if (loading) return <p className="text-sm text-carrot-50/50">Cargando pedidos...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  return (
    <div className="space-y-4">
      <div className="grid gap-4 sm:grid-cols-3">
        <StatCard label="Pedidos totales" value={rows?.length ?? 0} />
        <StatCard
          label="Ingresos (pagados)"
          value={formatMXN((rows ?? []).filter((o) => o.paymentStatus === "paid").reduce((s, o) => s + o.total, 0))}
        />
        <StatCard label="Pendientes" value={(rows ?? []).filter((o) => o.status === "pending").length} />
      </div>
      <Table head={["Pedido", "Items", "Entrega", "Pago", "Total", "Estado", "Fecha"]}>
        {rows?.map((o) => (
          <tr key={o.id} className="hover:bg-carrot-50/5">
            <td className="px-4 py-3 font-mono text-xs text-carrot-50/60">#{o.id.slice(0, 8)}</td>
            <td className="max-w-xs px-4 py-3 text-xs text-carrot-50/60">
              {o.items.map((i) => `${i.qty}x ${i.name}`).join(", ")}
            </td>
            <td className="px-4 py-3 text-xs text-carrot-50/60">{o.fulfillmentType === "pickup" ? "Recoger" : "Domicilio"}</td>
            <td className="px-4 py-3">
              <Badge tone={o.paymentStatus === "paid" ? "good" : "neutral"}>
                {o.paymentMethod === "card" ? "Tarjeta" : "Efectivo"} · {o.paymentStatus}
              </Badge>
            </td>
            <td className="px-4 py-3 font-semibold text-carrot-50">{formatMXN(o.total)}</td>
            <td className="px-4 py-3">
              <StatusSelect
                value={o.status}
                options={["pending", "preparing", "ready", "delivered", "cancelled"]}
                onChange={(v) => changeStatus(o.id, v)}
              />
            </td>
            <td className="px-4 py-3 text-xs text-carrot-50/50">{new Date(o.createdAt).toLocaleString("es-MX")}</td>
          </tr>
        ))}
      </Table>
    </div>
  );
}

function WholesaleSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminWholesaleOrders);
  const [rows, setRows] = useState(data);
  useEffect(() => setRows(data), [data]);

  async function changeStatus(id: string, status: string) {
    setRows((prev) => prev?.map((o) => (o.id === id ? { ...o, status } : o)) ?? null);
    await updateWholesaleStatus(id, status).catch(() => {});
  }

  if (loading) return <p className="text-sm text-carrot-50/50">Cargando cotizaciones...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  return (
    <Table head={["Cliente", "Producto", "Unidades", "Cotización", "Entrega", "Estado", "Fecha"]}>
      {rows?.map((o) => (
        <tr key={o.id} className="hover:bg-carrot-50/5">
          <td className="px-4 py-3">
            <p className="text-carrot-50">{o.customerName}</p>
            <p className="text-xs text-carrot-50/50">
              {o.phone} {o.company ? `· ${o.company}` : ""}
            </p>
          </td>
          <td className="px-4 py-3 text-carrot-50/80">{o.itemName}</td>
          <td className="px-4 py-3 text-carrot-50/80">{o.units}</td>
          <td className="px-4 py-3 font-semibold text-carrot-50">{o.quoteTotal ? formatMXN(o.quoteTotal) : "—"}</td>
          <td className="px-4 py-3 text-xs text-carrot-50/60">{o.fulfillmentType === "pickup" ? "Recoger" : "Domicilio"}</td>
          <td className="px-4 py-3">
            <StatusSelect value={o.status} options={["pending", "confirmed", "cancelled"]} onChange={(v) => changeStatus(o.id, v)} />
          </td>
          <td className="px-4 py-3 text-xs text-carrot-50/50">{new Date(o.createdAt).toLocaleDateString("es-MX")}</td>
        </tr>
      ))}
    </Table>
  );
}

function SurveysSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminSurveys);
  if (loading) return <p className="text-sm text-carrot-50/50">Cargando encuestas...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  const avgBranch = data?.length ? (data.reduce((s, r) => s + r.branchRating, 0) / data.length).toFixed(1) : "—";
  const avgService = data?.length ? (data.reduce((s, r) => s + r.serviceRating, 0) / data.length).toFixed(1) : "—";

  return (
    <div className="space-y-4">
      <div className="grid gap-4 sm:grid-cols-3">
        <StatCard label="Encuestas recibidas" value={data?.length ?? 0} />
        <StatCard label="Calificación sucursal" value={`${avgBranch} ★`} />
        <StatCard label="Calificación servicio" value={`${avgService} ★`} />
      </div>
      <Table head={["Sucursal", "Servicio", "Cajero(a)", "Comentarios", "Fecha"]}>
        {data?.map((s) => (
          <tr key={s.id} className="hover:bg-carrot-50/5">
            <td className="px-4 py-3 text-carrot-50">{s.branchRating} ★</td>
            <td className="px-4 py-3 text-carrot-50">{s.serviceRating} ★</td>
            <td className="px-4 py-3 text-carrot-50/80">{s.cashierName ?? "—"}</td>
            <td className="max-w-sm px-4 py-3 text-xs text-carrot-50/60">{s.comments ?? "—"}</td>
            <td className="px-4 py-3 text-xs text-carrot-50/50">{new Date(s.createdAt).toLocaleDateString("es-MX")}</td>
          </tr>
        ))}
      </Table>
    </div>
  );
}

function CustomersSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminCustomers);
  if (loading) return <p className="text-sm text-carrot-50/50">Cargando clientes...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  return (
    <div className="space-y-4">
      <div className="grid gap-4 sm:grid-cols-3">
        <StatCard label="Miembros de fidelización" value={data?.length ?? 0} />
        <StatCard label="Puntos en circulación" value={(data ?? []).reduce((s, c) => s + c.points, 0)} />
        <StatCard label="Saldo en monederos" value={formatMXN((data ?? []).reduce((s, c) => s + c.walletBalance, 0))} />
      </div>
      <Table head={["Cliente", "Contacto", "Código", "Puntos", "Monedero", "Desde"]}>
        {data?.map((c) => (
          <tr key={c.id} className="hover:bg-carrot-50/5">
            <td className="px-4 py-3 text-carrot-50">{c.name}</td>
            <td className="px-4 py-3 text-xs text-carrot-50/60">{c.email ?? c.phone}</td>
            <td className="px-4 py-3 font-mono text-xs text-carrot-50/60">{c.memberCode}</td>
            <td className="px-4 py-3 text-carrot-50">{c.points}</td>
            <td className="px-4 py-3 text-carrot-50">{formatMXN(c.walletBalance)}</td>
            <td className="px-4 py-3 text-xs text-carrot-50/50">{new Date(c.createdAt).toLocaleDateString("es-MX")}</td>
          </tr>
        ))}
      </Table>
    </div>
  );
}

function JobApplicationsSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminJobApplications);
  const [rows, setRows] = useState(data);
  useEffect(() => setRows(data), [data]);

  async function changeStatus(id: string, status: string) {
    setRows((prev) => prev?.map((o) => (o.id === id ? { ...o, status } : o)) ?? null);
    await updateJobApplicationStatus(id, status).catch(() => {});
  }

  if (loading) return <p className="text-sm text-carrot-50/50">Cargando solicitudes...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  return (
    <Table head={["Candidato", "Contacto", "Puesto", "Estado", "Fecha"]}>
      {rows?.map((a) => (
        <tr key={a.id} className="hover:bg-carrot-50/5">
          <td className="px-4 py-3 text-carrot-50">{a.name}</td>
          <td className="px-4 py-3 text-xs text-carrot-50/60">{a.phone ?? a.email ?? "—"}</td>
          <td className="px-4 py-3 text-carrot-50/80">{a.positionTitle ?? "—"}</td>
          <td className="px-4 py-3">
            <StatusSelect
              value={a.status}
              options={["received", "reviewing", "hired", "rejected"]}
              onChange={(v) => changeStatus(a.id, v)}
            />
          </td>
          <td className="px-4 py-3 text-xs text-carrot-50/50">{new Date(a.createdAt).toLocaleDateString("es-MX")}</td>
        </tr>
      ))}
    </Table>
  );
}

/**
 * The bonus becomes claimable once the candidate reaches 3 months of
 * tenure (hireDate + 3mo, i.e. bonusClaimDeadline − 7d) and stays claimable
 * for a 7-day window ending at bonusClaimDeadline.
 */
function referralBonusState(r: AdminReferral): "not-hired" | "not-yet" | "claimable" | "expired" | "claimed" {
  if (r.bonusClaimed) return "claimed";
  if (r.status !== "hired" || !r.hireDate || !r.bonusClaimDeadline) return "not-hired";
  const today = new Date().toISOString().slice(0, 10);
  const eligibleFrom = new Date(r.bonusClaimDeadline);
  eligibleFrom.setDate(eligibleFrom.getDate() - 7);
  const eligibleFromStr = eligibleFrom.toISOString().slice(0, 10);
  if (today < eligibleFromStr) return "not-yet";
  if (today > r.bonusClaimDeadline) return "expired";
  return "claimable";
}

function ReferralsSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminReferrals);
  const [rows, setRows] = useState(data);
  useEffect(() => setRows(data), [data]);

  async function refresh() {
    const fresh = await fetchAdminReferrals().catch(() => null);
    if (fresh) setRows(fresh);
  }

  async function changeStatus(id: string, status: string) {
    if (status === "hired") {
      // Sets hire_date + bonus_claim_deadline server-side, so refetch instead
      // of guessing those dates optimistically.
      await markReferralHired(id).catch(() => {});
      await refresh();
      return;
    }
    setRows((prev) => prev?.map((o) => (o.id === id ? { ...o, status } : o)) ?? null);
    await updateReferralStatus(id, status).catch(() => {});
  }

  async function claimBonus(id: string) {
    setRows((prev) => prev?.map((o) => (o.id === id ? { ...o, bonusClaimed: true } : o)) ?? null);
    await claimReferralBonus(id).catch(() => {});
  }

  if (loading) return <p className="text-sm text-carrot-50/50">Cargando referidos...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  return (
    <Table head={["Candidato", "Teléfono", "Puesto", "Estado", "Bono", "Fecha"]}>
      {rows?.map((r) => {
        const bonusState = referralBonusState(r);
        return (
          <tr key={r.id} className="hover:bg-carrot-50/5">
            <td className="px-4 py-3 text-carrot-50">{r.candidateName}</td>
            <td className="px-4 py-3 text-xs text-carrot-50/60">{r.candidatePhone ?? "—"}</td>
            <td className="px-4 py-3 text-carrot-50/80">{r.positionTitle ?? "—"}</td>
            <td className="px-4 py-3">
              <StatusSelect value={r.status} options={["pending", "hired", "rejected"]} onChange={(v) => changeStatus(r.id, v)} />
            </td>
            <td className="px-4 py-3">
              {bonusState === "claimed" && <Badge tone="good">Reclamado ✓</Badge>}
              {bonusState === "not-hired" && <Badge tone="neutral">—</Badge>}
              {bonusState === "not-yet" && r.hireDate && (
                <Badge tone="neutral">
                  Disponible el{" "}
                  {new Date(new Date(r.bonusClaimDeadline!).setDate(new Date(r.bonusClaimDeadline!).getDate() - 7)).toLocaleDateString(
                    "es-MX"
                  )}
                </Badge>
              )}
              {bonusState === "expired" && <Badge tone="bad">Vencido</Badge>}
              {bonusState === "claimable" && (
                <button
                  type="button"
                  onClick={() => claimBonus(r.id)}
                  className="rounded-full bg-leaf-500 px-3 py-1 text-[11px] font-semibold text-ink-950 hover:bg-leaf-400"
                >
                  Reclamar bono
                </button>
              )}
            </td>
            <td className="px-4 py-3 text-xs text-carrot-50/50">{new Date(r.createdAt).toLocaleDateString("es-MX")}</td>
          </tr>
        );
      })}
    </Table>
  );
}

function ReviewsSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminGoogleReviewsCount);
  if (loading) return <p className="text-sm text-carrot-50/50">Cargando...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;
  return (
    <div className="grid gap-4 sm:grid-cols-3">
      <StatCard label="Clics a reseña de Google" value={data ?? 0} hint="Registrados desde la página de encuestas" />
    </div>
  );
}

function CouponsSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminCoupons);
  const [rows, setRows] = useState(data);
  const [showForm, setShowForm] = useState(false);
  const [form, setForm] = useState({ code: "", title: "", description: "", discountLabel: "", discountPercent: "" });
  const [creating, setCreating] = useState(false);
  const [formError, setFormError] = useState("");
  useEffect(() => setRows(data), [data]);

  async function toggle(id: string, active: boolean) {
    setRows((prev) => prev?.map((c) => (c.id === id ? { ...c, active } : c)) ?? null);
    await toggleCouponActive(id, active).catch(() => {});
  }

  async function handleCreate(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setCreating(true);
    setFormError("");
    try {
      await createCoupon({
        code: form.code.trim(),
        title: form.title.trim(),
        description: form.description.trim(),
        discountLabel: form.discountLabel.trim() || `${form.discountPercent}% de descuento`,
        discountPercent: form.discountPercent ? Number(form.discountPercent) : null,
        validUntil: null,
        onlyOnline: true,
      });
      setForm({ code: "", title: "", description: "", discountLabel: "", discountPercent: "" });
      setShowForm(false);
      const fresh = await fetchAdminCoupons();
      setRows(fresh);
    } catch (err) {
      setFormError(err instanceof Error ? err.message : "No se pudo crear el cupón.");
    } finally {
      setCreating(false);
    }
  }

  if (loading) return <p className="text-sm text-carrot-50/50">Cargando cupones...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  return (
    <div className="space-y-4">
      <div className="flex justify-end">
        <button
          type="button"
          onClick={() => setShowForm((v) => !v)}
          className="flex items-center gap-1.5 rounded-full bg-carrot-500 px-3.5 py-1.5 text-xs font-semibold text-ink-950 hover:bg-carrot-400"
        >
          <Plus size={14} /> Nuevo cupón
        </button>
      </div>

      {showForm && (
        <form onSubmit={handleCreate} className="glass grid gap-3 rounded-2xl p-4 sm:grid-cols-2">
          <input
            required
            placeholder="Código (ej. BIENVENIDO10)"
            value={form.code}
            onChange={(e) => setForm({ ...form, code: e.target.value.toUpperCase() })}
            className="rounded-xl border border-carrot-50/15 bg-carrot-50/10 px-3 py-2 text-sm text-carrot-50 placeholder:text-carrot-50/40 focus:outline-none focus:ring-1 focus:ring-carrot-400"
          />
          <input
            required
            placeholder="Título"
            value={form.title}
            onChange={(e) => setForm({ ...form, title: e.target.value })}
            className="rounded-xl border border-carrot-50/15 bg-carrot-50/10 px-3 py-2 text-sm text-carrot-50 placeholder:text-carrot-50/40 focus:outline-none focus:ring-1 focus:ring-carrot-400"
          />
          <input
            placeholder="Descripción"
            value={form.description}
            onChange={(e) => setForm({ ...form, description: e.target.value })}
            className="rounded-xl border border-carrot-50/15 bg-carrot-50/10 px-3 py-2 text-sm text-carrot-50 placeholder:text-carrot-50/40 focus:outline-none focus:ring-1 focus:ring-carrot-400 sm:col-span-2"
          />
          <input
            type="number"
            placeholder="% de descuento"
            value={form.discountPercent}
            onChange={(e) => setForm({ ...form, discountPercent: e.target.value })}
            className="rounded-xl border border-carrot-50/15 bg-carrot-50/10 px-3 py-2 text-sm text-carrot-50 placeholder:text-carrot-50/40 focus:outline-none focus:ring-1 focus:ring-carrot-400"
          />
          <input
            placeholder="Etiqueta (ej. 10% de descuento)"
            value={form.discountLabel}
            onChange={(e) => setForm({ ...form, discountLabel: e.target.value })}
            className="rounded-xl border border-carrot-50/15 bg-carrot-50/10 px-3 py-2 text-sm text-carrot-50 placeholder:text-carrot-50/40 focus:outline-none focus:ring-1 focus:ring-carrot-400"
          />
          {formError && <p className="text-xs text-red-400 sm:col-span-2">{formError}</p>}
          <button
            type="submit"
            disabled={creating}
            className="rounded-full bg-carrot-500 px-3 py-2 text-xs font-semibold text-ink-950 hover:bg-carrot-400 disabled:opacity-40 sm:col-span-2"
          >
            {creating ? "Creando..." : "Crear cupón"}
          </button>
        </form>
      )}

      <Table head={["Código", "Título", "Descuento", "Solo web", "Activo"]}>
        {rows?.map((c) => (
          <tr key={c.id} className="hover:bg-carrot-50/5">
            <td className="px-4 py-3 font-mono text-xs text-carrot-50/80">{c.code}</td>
            <td className="px-4 py-3 text-carrot-50">{c.title}</td>
            <td className="px-4 py-3 text-xs text-carrot-50/60">{c.discountLabel}</td>
            <td className="px-4 py-3">
              <Badge tone={c.onlyOnline ? "good" : "neutral"}>{c.onlyOnline ? "Sí" : "No"}</Badge>
            </td>
            <td className="px-4 py-3">
              <button
                type="button"
                onClick={() => toggle(c.id, !c.active)}
                className={cn(
                  "rounded-full px-3 py-1 text-[11px] font-semibold",
                  c.active ? "bg-leaf-500/15 text-leaf-300" : "bg-carrot-50/10 text-carrot-50/50"
                )}
              >
                {c.active ? "Activo" : "Inactivo"}
              </button>
            </td>
          </tr>
        ))}
      </Table>
    </div>
  );
}

function ProductRow({ product, onSaved }: { product: Awaited<ReturnType<typeof fetchAdminProducts>>[number]; onSaved: () => void }) {
  const [priceMedium, setPriceMedium] = useState(product.priceMedium?.toString() ?? "");
  const [priceLarge, setPriceLarge] = useState(product.priceLarge?.toString() ?? "");
  const [active, setActive] = useState(product.active);
  const [saving, setSaving] = useState(false);
  const [dirty, setDirty] = useState(false);

  async function save() {
    setSaving(true);
    try {
      await updateProductPricing(product.id, {
        priceMedium: priceMedium ? Number(priceMedium) : null,
        priceLarge: priceLarge ? Number(priceLarge) : null,
        active,
      });
      setDirty(false);
      onSaved();
    } catch {
      /* keep local edits so the user can retry */
    } finally {
      setSaving(false);
    }
  }

  return (
    <tr className="hover:bg-carrot-50/5">
      <td className="px-4 py-3 text-carrot-50">{product.name}</td>
      <td className="px-4 py-3 text-xs text-carrot-50/60">{product.categoryName}</td>
      <td className="px-4 py-3">
        <input
          type="number"
          value={priceMedium}
          onChange={(e) => {
            setPriceMedium(e.target.value);
            setDirty(true);
          }}
          className="w-24 rounded-lg border border-carrot-50/15 bg-carrot-50/10 px-2 py-1 text-xs text-carrot-50 focus:outline-none focus:ring-1 focus:ring-carrot-400"
        />
      </td>
      <td className="px-4 py-3">
        <input
          type="number"
          value={priceLarge}
          onChange={(e) => {
            setPriceLarge(e.target.value);
            setDirty(true);
          }}
          className="w-24 rounded-lg border border-carrot-50/15 bg-carrot-50/10 px-2 py-1 text-xs text-carrot-50 focus:outline-none focus:ring-1 focus:ring-carrot-400"
        />
      </td>
      <td className="px-4 py-3">
        <button
          type="button"
          onClick={() => {
            setActive((v) => !v);
            setDirty(true);
          }}
          className={cn(
            "rounded-full px-3 py-1 text-[11px] font-semibold",
            active ? "bg-leaf-500/15 text-leaf-300" : "bg-carrot-50/10 text-carrot-50/50"
          )}
        >
          {active ? "Activo" : "Inactivo"}
        </button>
      </td>
      <td className="px-4 py-3">
        <button
          type="button"
          onClick={save}
          disabled={!dirty || saving}
          className="rounded-full bg-carrot-500 px-3 py-1.5 text-xs font-semibold text-ink-950 hover:bg-carrot-400 disabled:opacity-30"
        >
          {saving ? "Guardando..." : "Guardar"}
        </button>
      </td>
    </tr>
  );
}

function ProductsSection() {
  const { data, error, loading } = useSupabaseData(fetchAdminProducts);
  const [refreshKey, setRefreshKey] = useState(0);

  if (loading) return <p className="text-sm text-carrot-50/50">Cargando productos...</p>;
  if (error) return <p className="text-sm text-red-400">{error}</p>;

  return (
    <Table head={["Producto", "Categoría", "Precio mediano", "Precio grande", "Activo", ""]}>
      {data?.map((p) => (
        <ProductRow key={`${p.id}-${refreshKey}`} product={p} onSaved={() => setRefreshKey((k) => k + 1)} />
      ))}
    </Table>
  );
}

function OverviewSection({ goTo }: { goTo: (s: Section) => void }) {
  const { data: orders } = useSupabaseData(fetchAdminOrders);
  const { data: wholesale } = useSupabaseData(fetchAdminWholesaleOrders);
  const { data: customers } = useSupabaseData(fetchAdminCustomers);
  const { data: applications } = useSupabaseData(fetchAdminJobApplications);

  return (
    <div className="space-y-6">
      <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
        <StatCard
          label="Ingresos pagados"
          value={formatMXN((orders ?? []).filter((o) => o.paymentStatus === "paid").reduce((s, o) => s + o.total, 0))}
        />
        <StatCard label="Pedidos" value={orders?.length ?? 0} hint={`${(orders ?? []).filter((o) => o.status === "pending").length} pendientes`} />
        <StatCard label="Cotizaciones de mayoreo" value={wholesale?.length ?? 0} />
        <StatCard label="Miembros de fidelización" value={customers?.length ?? 0} />
      </div>
      <div className="grid gap-4 lg:grid-cols-2">
        <button
          type="button"
          onClick={() => goTo("empleo")}
          className="glass glass-card rounded-2xl p-5 text-left"
        >
          <p className="text-xs font-medium text-carrot-50/60">Solicitudes de empleo por revisar</p>
          <p className="mt-2 text-2xl font-bold text-carrot-50">
            {(applications ?? []).filter((a) => a.status === "received").length}
          </p>
        </button>
        <button
          type="button"
          onClick={() => goTo("pedidos")}
          className="glass glass-card rounded-2xl p-5 text-left"
        >
          <p className="text-xs font-medium text-carrot-50/60">Ir a la cola de pedidos</p>
          <p className="mt-2 text-sm text-carrot-50/70">Cambia el estado de preparación y entrega en tiempo real.</p>
        </button>
      </div>
    </div>
  );
}

export default function AdminDashboard() {
  const { employee, loading: sessionLoading, login, logout } = useStaffSession();
  const [section, setSection] = useState<Section>("resumen");
  const [sidebarOpen, setSidebarOpen] = useState(false);

  if (sessionLoading) return <p className="text-sm text-carrot-50/50">Cargando sesión...</p>;
  if (!employee) return <StaffLogin onLogin={login} title="Acceso al panel administrativo" />;
  if (employee.role !== "admin") {
    return (
      <div className="glass mx-auto max-w-md rounded-2xl p-8 text-center">
        <p className="text-lg font-semibold">No tienes permisos de administrador</p>
        <p className="mt-2 text-sm text-carrot-50/60">
          Tu cuenta ({employee.name}) tiene rol de personal. Pide a un administrador que actualice tu rol.
        </p>
        <button onClick={logout} className="mt-4 rounded-full bg-carrot-500 px-5 py-2 text-sm font-semibold text-ink-950">
          Cerrar sesión
        </button>
      </div>
    );
  }

  return (
    <div className="fixed inset-0 z-40 flex text-carrot-50">
      {/* Sidebar */}
      <aside
        className={cn(
          "glass-strong fixed inset-y-0 left-0 z-50 w-64 shrink-0 rounded-r-3xl transition-transform md:static md:translate-x-0 md:rounded-none md:border-r md:border-carrot-50/10 md:bg-transparent md:backdrop-blur-none md:shadow-none",
          sidebarOpen ? "translate-x-0" : "-translate-x-full"
        )}
      >
        <div className="flex h-14 items-center gap-2 border-b border-carrot-50/10 px-4">
          <span className="grid h-8 w-8 place-items-center rounded-full bg-gradient-to-br from-carrot-400 to-leaf-500 text-base shadow-glass">
            🥕
          </span>
          <span className="text-sm font-semibold text-carrot-50">Don Zanahorio Admin</span>
        </div>
        <nav className="space-y-6 overflow-y-auto p-3">
          {NAV_GROUPS.map((group) => (
            <div key={group.label}>
              <p className="px-3 text-[11px] font-semibold uppercase tracking-wide text-carrot-50/40">{group.label}</p>
              <div className="mt-1 space-y-0.5">
                {group.items.map((item) => (
                  <button
                    key={item.id}
                    type="button"
                    onClick={() => {
                      setSection(item.id);
                      setSidebarOpen(false);
                    }}
                    className={cn(
                      "flex w-full items-center gap-3 rounded-full px-3 py-2 text-sm transition",
                      section === item.id
                        ? "bg-carrot-500/20 text-carrot-200"
                        : "text-carrot-50/60 hover:bg-carrot-50/10 hover:text-carrot-100"
                    )}
                  >
                    <item.icon size={16} />
                    {item.label}
                  </button>
                ))}
              </div>
            </div>
          ))}
        </nav>
      </aside>

      {sidebarOpen && (
        <div className="fixed inset-0 z-40 bg-ink-950/70 md:hidden" onClick={() => setSidebarOpen(false)} />
      )}

      {/* Main */}
      <div className="flex min-w-0 flex-1 flex-col">
        <header className="glass-strong flex h-14 items-center gap-3 rounded-none border-x-0 border-t-0 px-4">
          <button type="button" onClick={() => setSidebarOpen((v) => !v)} className="text-carrot-50/70 hover:text-carrot-100 md:hidden">
            {sidebarOpen ? <X size={20} /> : <Menu size={20} />}
          </button>
          <div className="h-6 w-px bg-carrot-50/15" />
          <h1 className="text-sm font-medium text-carrot-50">{SECTION_LABELS[section]}</h1>
          <div className="ml-auto flex items-center gap-3">
            <div className="text-right">
              <p className="text-xs font-medium text-carrot-100">{employee.name}</p>
              <p className="text-[11px] text-carrot-50/50">Administrador</p>
            </div>
            <button
              type="button"
              onClick={logout}
              className="glass glass-card flex items-center gap-1.5 rounded-full px-3 py-1.5 text-xs text-carrot-50/80"
            >
              <LogOut size={13} /> Salir
            </button>
          </div>
        </header>

        <main className="flex-1 overflow-y-auto p-6">
          {section === "resumen" && <OverviewSection goTo={setSection} />}
          {section === "pedidos" && <OrdersSection />}
          {section === "mayoreo" && <WholesaleSection />}
          {section === "encuestas" && <SurveysSection />}
          {section === "clientes" && <CustomersSection />}
          {section === "empleo" && <JobApplicationsSection />}
          {section === "referidos" && <ReferralsSection />}
          {section === "cupones" && <CouponsSection />}
          {section === "productos" && <ProductsSection />}
          {section === "resenas" && <ReviewsSection />}
        </main>
      </div>
    </div>
  );
}
</file>

<file path="src/components/islands/StaffLogin.tsx">
import { useState } from "react";

export default function StaffLogin({
  onLogin,
  title = "Acceso de empleados",
}: {
  onLogin: (email: string, password: string) => Promise<void>;
  title?: string;
}) {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState("");

  return (
    <form
      className="glass mx-auto max-w-sm space-y-4 rounded-2xl p-6"
      onSubmit={async (e) => {
        e.preventDefault();
        setSubmitting(true);
        setError("");
        try {
          await onLogin(email.trim(), password);
        } catch (err) {
          setError(err instanceof Error ? err.message : "Correo o contraseña incorrectos.");
        } finally {
          setSubmitting(false);
        }
      }}
    >
      <div className="text-center">
        <span className="text-3xl">🔐</span>
        <h2 className="mt-2 text-lg font-bold">{title}</h2>
        <p className="mt-1 text-xs text-carrot-50/60">
          Demo: admin@donzanahorio.com o cajero@donzanahorio.com — contraseña DonZanahorio2026!
        </p>
      </div>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Correo</label>
        <input
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          required
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
        />
      </div>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Contraseña</label>
        <input
          type="password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          required
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
        />
      </div>
      {error && <p className="text-xs text-red-400">{error}</p>}
      <button
        type="submit"
        disabled={submitting}
        className="w-full rounded-full bg-carrot-500 py-2.5 text-sm font-semibold text-ink-950 hover:bg-carrot-400 disabled:cursor-not-allowed disabled:opacity-40"
      >
        {submitting ? "Entrando..." : "Entrar"}
      </button>
    </form>
  );
}
</file>

<file path="src/hooks/useMounted.ts">
import { useEffect, useState } from "react";

/**
 * True once the component has completed its first client-side commit.
 *
 * Astro server-renders islands without access to localStorage, so any UI
 * derived from a persistent store (cart contents, loyalty session, ...)
 * must render its SSR-safe default on the very first client render too —
 * otherwise React's hydration pass sees a mismatch between server and
 * client markup and discards/regenerates the whole subtree.
 */
export function useMounted() {
  const [mounted, setMounted] = useState(false);
  useEffect(() => setMounted(true), []);
  return mounted;
}
</file>

<file path="src/hooks/useStaffSession.ts">
import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";

export type StaffEmployee = { id: string; name: string; email: string; role: "staff" | "admin" };

export function useStaffSession() {
  const [employee, setEmployee] = useState<StaffEmployee | null | undefined>(undefined);

  async function loadEmployee(userId: string) {
    const { data } = await supabase
      .from("employees")
      .select("id, name, email, role")
      .eq("auth_user_id", userId)
      .single();
    setEmployee((data as StaffEmployee) ?? null);
  }

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      if (data.session?.user) loadEmployee(data.session.user.id);
      else setEmployee(null);
    });
    const { data: sub } = supabase.auth.onAuthStateChange((_event, session) => {
      if (session?.user) loadEmployee(session.user.id);
      else setEmployee(null);
    });
    return () => sub.subscription.unsubscribe();
  }, []);

  async function login(email: string, password: string) {
    const { error } = await supabase.auth.signInWithPassword({ email, password });
    if (error) throw new Error("Correo o contraseña incorrectos.");
  }

  async function logout() {
    await supabase.auth.signOut();
    setEmployee(null);
  }

  return { employee, loading: employee === undefined, login, logout };
}
</file>

<file path="src/layouts/AdminLayout.astro">
---
import "@/styles/global.css";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex" />
    <meta name="generator" content={Astro.generator} />
    <title>{title} · Don Zanahorio Admin</title>
  </head>
  <body>
    <div class="ambient-bg">
      <div class="ambient-blob h-[26rem] w-[26rem] bg-carrot-500 -left-20 -top-20"></div>
      <div class="ambient-blob h-[22rem] w-[22rem] bg-leaf-500 -right-10 top-1/3" style="animation-delay: -6s"></div>
      <div class="ambient-blob h-[24rem] w-[24rem] bg-carrot-700 left-1/3 -bottom-24" style="animation-delay: -12s"></div>
    </div>
    <slot />
  </body>
</html>
</file>

<file path="src/lib/subscriptionDiscount.ts">
/**
 * Preferential pricing granted by an active subscription plan (section 2.3
 * of the client's requirements: "aplicable para consumo en sucursal o
 * servicio a domicilio"). Shared between the online cart and POS so the
 * same customer gets the same benefit either way.
 *
 * comida-semanal's own perk ("20% de descuento en pedidos de mayoreo") is
 * intentionally not handled here — it applies to wholesale quotes, not a
 * line-item purchase, and Mayoreo has no customer-lookup field yet.
 */
export type SubscriptionDiscount = { amount: number; label: string };

export function computeSubscriptionDiscount(
  planSlug: string | null | undefined,
  lines: Array<{ name: string; price: number }>,
  subtotal: number
): SubscriptionDiscount | null {
  if (!planSlug || subtotal <= 0) return null;

  if (planSlug === "jugo-diario") {
    return { amount: Math.round(subtotal * 0.1), label: "Jugo Diario: 10% en el resto del menú" };
  }

  if (planSlug === "cafe-diario") {
    const coffeeLine = lines.find((l) => /caf[eé]/i.test(l.name));
    if (!coffeeLine) return null;
    return { amount: Math.round(coffeeLine.price), label: "Café Diario: 1 café gratis hoy" };
  }

  return null;
}
</file>

<file path="src/pages/api/checkout/create-session.ts">
import type { APIRoute } from "astro";
import Stripe from "stripe";

export const prerender = false;

export const POST: APIRoute = async ({ request }) => {
  const secretKey = import.meta.env.STRIPE_SECRET_KEY;
  if (!secretKey) {
    return new Response(
      JSON.stringify({ error: "Los pagos con tarjeta no están configurados en este entorno (falta STRIPE_SECRET_KEY)." }),
      { status: 500, headers: { "Content-Type": "application/json" } }
    );
  }

  const body = (await request.json()) as {
    orderId: string;
    items: Array<{ name: string; price: number; qty: number }>;
    shippingCost: number;
    discount: number;
  };

  if (!body.orderId || !Array.isArray(body.items) || body.items.length === 0) {
    return new Response(JSON.stringify({ error: "Pedido inválido." }), {
      status: 400,
      headers: { "Content-Type": "application/json" },
    });
  }

  const stripe = new Stripe(secretKey);
  const origin = request.headers.get("origin") ?? new URL(request.url).origin;

  const lineItems: Stripe.Checkout.SessionCreateParams.LineItem[] = body.items.map((item) => ({
    quantity: item.qty,
    price_data: {
      currency: "mxn",
      unit_amount: Math.round(item.price * 100),
      product_data: { name: item.name },
    },
  }));

  if (body.shippingCost > 0) {
    lineItems.push({
      quantity: 1,
      price_data: {
        currency: "mxn",
        unit_amount: Math.round(body.shippingCost * 100),
        product_data: { name: "Envío" },
      },
    });
  }

  let discounts: Stripe.Checkout.SessionCreateParams.Discount[] | undefined;
  if (body.discount > 0) {
    const coupon = await stripe.coupons.create({
      amount_off: Math.round(body.discount * 100),
      currency: "mxn",
      duration: "once",
    });
    discounts = [{ coupon: coupon.id }];
  }

  try {
    const session = await stripe.checkout.sessions.create({
      mode: "payment",
      line_items: lineItems,
      discounts,
      success_url: `${origin}/carrito/exito?session_id={CHECKOUT_SESSION_ID}&order=${body.orderId}`,
      cancel_url: `${origin}/carrito`,
      metadata: { orderId: body.orderId },
    });

    return new Response(JSON.stringify({ url: session.url }), {
      status: 200,
      headers: { "Content-Type": "application/json" },
    });
  } catch (err) {
    return new Response(JSON.stringify({ error: err instanceof Error ? err.message : "No se pudo iniciar el pago." }), {
      status: 500,
      headers: { "Content-Type": "application/json" },
    });
  }
};
</file>

<file path="src/pages/carrito/exito.astro">
---
export const prerender = false;

import Layout from "@/layouts/Layout.astro";
import Stripe from "stripe";
import { markOrderPaid } from "@/lib/queries";

const sessionId = Astro.url.searchParams.get("session_id");
const orderId = Astro.url.searchParams.get("order");

let paid = false;
let errorMessage = "";

if (!sessionId || !orderId) {
  errorMessage = "Faltan datos de la sesión de pago.";
} else if (!import.meta.env.STRIPE_SECRET_KEY) {
  errorMessage = "Los pagos con tarjeta no están configurados en este entorno.";
} else {
  try {
    const stripe = new Stripe(import.meta.env.STRIPE_SECRET_KEY);
    const session = await stripe.checkout.sessions.retrieve(sessionId);
    if (session.payment_status === "paid") {
      await markOrderPaid(orderId, sessionId, import.meta.env.INTERNAL_RPC_SECRET);
      paid = true;
    } else {
      errorMessage = "El pago no se completó todavía.";
    }
  } catch (err) {
    errorMessage = err instanceof Error ? err.message : "No se pudo verificar el pago.";
  }
}
---

<Layout title="Pago confirmado" description="Confirmación de pago de tu pedido en Don Zanahorio.">
  <div class="glass-strong mx-auto max-w-lg rounded-3xl p-8 text-center">
    {paid ? (
      <>
        <span class="text-5xl">✅</span>
        <h1 class="mt-4 text-2xl font-bold">¡Pago confirmado!</h1>
        <p class="mt-2 text-sm text-carrot-50/70">
          Tu pedido #{orderId?.slice(0, 8)} fue pagado con éxito. Te esperamos en Don Zanahorio.
        </p>
      </>
    ) : (
      <>
        <span class="text-5xl">⚠️</span>
        <h1 class="mt-4 text-2xl font-bold">No pudimos confirmar el pago</h1>
        <p class="mt-2 text-sm text-carrot-50/70">{errorMessage}</p>
      </>
    )}
    <div class="mt-6 flex flex-col gap-3 sm:flex-row sm:justify-center">
      <a
        href="/menu"
        class="rounded-full bg-carrot-500 px-6 py-3 text-sm font-semibold text-ink-950 hover:bg-carrot-400"
      >
        Seguir explorando el menú
      </a>
    </div>
  </div>
</Layout>
</file>

<file path="src/pages/admin.astro">
---
import AdminLayout from "@/layouts/AdminLayout.astro";
import AdminDashboard from "@/components/islands/AdminDashboard";
---

<AdminLayout title="Panel administrativo">
  <AdminDashboard client:load />
</AdminLayout>
</file>

<file path="playwright.config.ts">
import { defineConfig, devices } from "@playwright/test";

/**
 * E2E suite for the Don Zanahorio demo (Astro + React islands + Supabase).
 * Run against the local dev server: `pnpm test:e2e`.
 *
 * Some specs write real rows to Supabase (orders, POS sales, surveys, wholesale
 * quotes, job applications/referrals, admin price edits). Those are tagged
 * `@writes-data` in their titles. Use `pnpm test:e2e:safe` to skip them.
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: process.env.CI ? 2 : undefined,
  timeout: 45_000,
  expect: { timeout: 10_000 },
  reporter: [["list"], ["html", { open: "never" }]],
  use: {
    baseURL: process.env.E2E_BASE_URL ?? "http://localhost:4321",
    trace: "on-first-retry",
    screenshot: "only-on-failure",
    video: "retain-on-failure",
  },
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"] },
    },
  ],
  webServer: {
    command: "pnpm dev",
    url: "http://localhost:4321",
    reuseExistingServer: true,
    timeout: 120_000,
  },
});
</file>

<file path="requerimientos-don-zanahorio.md">
**Documento de Requerimientos para Desarrollo de Página Web**

**1. Objetivo General**

Desarrollar una plataforma web moderna e integrada con un sistema de punto de venta (POS), inventarios, fidelización de clientes, capacitación de personal y control administrativo que permita optimizar las operaciones del negocio y mejorar la experiencia del cliente.

**2. Requerimientos de la Página Web**

**2.1 Experiencia del Cliente**

* Diseño moderno, dinámico, responsivo y fácil de usar.
* Menú digital completo.
* Compra en línea con:
  + Tarjeta de crédito/débito.
  + Efectivo.
* Opción de recoger en tienda o solicitar entrega a domicilio.
* Configuración automática de costo de envío.
* Enlace directo a WhatsApp.

**2.2 Programa de Fidelización**

* Registro de clientes mediante:
  + Correo electrónico.
  + Número telefónico.
* Generación de código QR único por cliente.
* Historial de compras.
* Programa de recompensas con mínimo 10% de beneficio.
* Monedero electrónico.
* Gift Cards.
* Bonos por recomendación de clientes.
* Acumulación y consulta de puntos.

**2.3 Membresías y Suscripciones**

* Suscripciones mensuales.
* Ejemplo:
  + Café diario a precio preferencial.
* Validación mediante:
  + Código QR.
  + Número telefónico.
* Aplicable para consumo en sucursal o servicio a domicilio.

**2.4 Promociones**

* Generación automática de cupones de descuento.
* Descuentos exclusivos para compras web.
* Promociones especiales para productos nuevos.
* Sección destacada para:
  + Productos nuevos.
  + Productos más económicos.
  + Productos más vendidos.
  + Promociones vigentes.

**2.5 Evaluaciones y Encuestas**

* Encuestas de satisfacción.
* Evaluación de sucursal.
* Evaluación de servicio.
* Votación de cajero favorito.
* Sistema de incentivos y bonos para trabajadores basado en evaluaciones.

**2.6 Reseñas en Google**

* Integración con enlaces para calificar en Google.
* Generación de beneficios por participación.
* Registro de usuarios que realizaron la evaluación.
* Administración de promociones derivadas de reseñas.

**2.7 Pedidos Mayoreo**

* Formulario especializado para pedidos por volumen.
* Selección de:
  + Recolección en tienda.
  + Entrega a domicilio.
* Generación automática de cotizaciones y costos de envío.

**3. Portal para Empleados**

**3.1 Capacitación**

* Área exclusiva para empleados.
* Acceso mediante usuario y contraseña.
* Carga y visualización de:
  + Videos.
  + Cursos.
  + Manuales.
  + Procedimientos.
* Compatible con tabletas.

**3.2 Manuales y Puestos**

* Manual de actividades.
* Perfil de cada puesto.
* Procedimientos operativos.

**3.3 Recomendación de Empleados**

* Formulario para recomendar candidatos.
* Registro de postulantes.
* Validación automática del tiempo laborado.
* Bono por recomendación cuando el candidato cumpla 3 meses.
* Periodo máximo de reclamación del bono: 7 días posteriores al cumplimiento del plazo.

**3.4 Reclutamiento**

* Formulario de solicitud de empleo.
* Carga de CV y documentos.
</file>

<file path="public/favicon.svg">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 128 128">
    <path d="M50.4 78.5a75.1 75.1 0 0 0-28.5 6.9l24.2-65.7c.7-2 1.9-3.2 3.4-3.2h29c1.5 0 2.7 1.2 3.4 3.2l24.2 65.7s-11.6-7-28.5-7L67 45.5c-.4-1.7-1.6-2.8-2.9-2.8-1.3 0-2.5 1.1-2.9 2.7L50.4 78.5Zm-1.1 28.2Zm-4.2-20.2c-2 6.6-.6 15.8 4.2 20.2a17.5 17.5 0 0 1 .2-.7 5.5 5.5 0 0 1 5.7-4.5c2.8.1 4.3 1.5 4.7 4.7.2 1.1.2 2.3.2 3.5v.4c0 2.7.7 5.2 2.2 7.4a13 13 0 0 0 5.7 4.9v-.3l-.2-.3c-1.8-5.6-.5-9.5 4.4-12.8l1.5-1a73 73 0 0 0 3.2-2.2 16 16 0 0 0 6.8-11.4c.3-2 .1-4-.6-6l-.8.6-1.6 1a37 37 0 0 1-22.4 2.7c-5-.7-9.7-2-13.2-6.2Z" />
    <style>
        path { fill: #000; }
        @media (prefers-color-scheme: dark) {
            path { fill: #FFF; }
        }
    </style>
</svg>
</file>

<file path="src/components/islands/CartBadge.tsx">
import { useStore } from "@nanostores/react";
import { ShoppingBasket } from "lucide-react";
import { $cartCount } from "@/stores/cart";
import { useMounted } from "@/hooks/useMounted";

export default function CartBadge() {
  const count = useStore($cartCount);
  // Cart contents live in localStorage, which the server can't see: treat
  // the count as 0 until after hydration so the first client render matches
  // what was server-rendered.
  const displayCount = useMounted() ? count : 0;

  return (
    <a
      href="/carrito"
      aria-label="Ver carrito"
      className="glass glass-card relative flex h-11 w-11 items-center justify-center rounded-full text-carrot-100 hover:text-carrot-300"
    >
      <ShoppingBasket size={19} strokeWidth={2} />
      {displayCount > 0 && (
        <span className="absolute -right-1.5 -top-1.5 flex h-5 min-w-5 items-center justify-center rounded-full bg-carrot-500 px-1 text-[11px] font-bold text-ink-950 shadow-glass">
          {displayCount}
        </span>
      )}
    </a>
  );
}
</file>

<file path="src/components/islands/CartCheckout.tsx">
import { useEffect, useMemo, useState } from "react";
import { useStore } from "@nanostores/react";
import { Minus, Plus, ShoppingBasket, Trash2 } from "lucide-react";
import { $cartLines, $cartTotal, clearCart, removeFromCart, setQty } from "@/stores/cart";
import { SITE, whatsappLink } from "@/data/site";
import { fetchCoupons, submitOrder, awardOrderPoints, lookupLoyaltyCustomer } from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";
import { $loyaltyContact } from "@/stores/loyalty";
import { cn, formatMXN } from "@/lib/utils";
import { useMounted } from "@/hooks/useMounted";
import { computeSubscriptionDiscount } from "@/lib/subscriptionDiscount";

type Fulfillment = "pickup" | "delivery";
type Payment = "card" | "cash";

export default function CartCheckout() {
  const lines = useStore($cartLines);
  const subtotal = useStore($cartTotal);
  // Cart contents live in localStorage, invisible to the server: pretend the
  // cart is empty (the SSR-safe default) until after hydration so the first
  // client render matches the server-rendered markup.
  const mounted = useMounted();
  const displayLines = mounted ? lines : [];
  const savedContact = useStore($loyaltyContact);
  const { data: coupons } = useSupabaseData(fetchCoupons);

  const [fulfillment, setFulfillment] = useState<Fulfillment>("pickup");
  const [payment, setPayment] = useState<Payment>("card");
  const [address, setAddress] = useState("");
  const [loyaltyContact, setLoyaltyContact] = useState(savedContact ?? "");
  const [subscriberPlanId, setSubscriberPlanId] = useState<string | null>(null);
  const [couponInput, setCouponInput] = useState("");
  const [appliedCoupon, setAppliedCoupon] = useState<string | null>(null);
  const [couponNote, setCouponNote] = useState("");
  const [couponError, setCouponError] = useState("");
  const [confirmedOrder, setConfirmedOrder] = useState<number | null>(null);
  const [redirecting, setRedirecting] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState("");

  const shipping = useMemo(() => {
    if (fulfillment === "pickup" || lines.length === 0) return 0;
    if (subtotal >= SITE.shipping.freeThreshold) return 0;
    return SITE.shipping.baseCost;
  }, [fulfillment, subtotal, lines.length]);

  const discount = useMemo(() => {
    if (!appliedCoupon) return 0;
    const match = coupons?.find((c) => c.code === appliedCoupon);
    if (!match?.discountPercent) return 0;
    return Math.round(subtotal * (match.discountPercent / 100));
  }, [appliedCoupon, coupons, subtotal]);

  const subscriptionDiscount = useMemo(
    () => computeSubscriptionDiscount(subscriberPlanId, lines, subtotal),
    [subscriberPlanId, lines, subtotal]
  );

  const total = Math.max(subtotal + shipping - discount - (subscriptionDiscount?.amount ?? 0), 0);

  async function lookupSubscriberPlan(contact: string) {
    const trimmed = contact.trim();
    if (!trimmed) {
      setSubscriberPlanId(null);
      return;
    }
    try {
      const profile = await lookupLoyaltyCustomer(trimmed);
      setSubscriberPlanId(profile?.activePlanId ?? null);
    } catch {
      setSubscriberPlanId(null);
    }
  }

  // Auto-apply the discount if the shopper is already logged into loyalty
  // elsewhere on the site (their contact pre-fills this field).
  useEffect(() => {
    if (savedContact) lookupSubscriberPlan(savedContact);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  function applyCoupon() {
    const code = couponInput.trim().toUpperCase();
    const match = coupons?.find((c) => c.code === code);
    if (!match) {
      setCouponError("Cupón no válido o vencido.");
      setCouponNote("");
      setAppliedCoupon(null);
      return;
    }
    setCouponError("");
    setAppliedCoupon(code);
    setCouponNote(match.discountPercent ? "" : `Este cupón (${match.discount}) se aplica directamente en tienda.`);
  }

  async function confirmOrder() {
    if (fulfillment === "delivery" && address.trim().length < 6) return;
    setSubmitting(true);
    setSubmitError("");
    try {
      const totalDiscount = discount + (subscriptionDiscount?.amount ?? 0);
      const orderId = await submitOrder({
        fulfillmentType: fulfillment,
        address: fulfillment === "delivery" ? address.trim() : null,
        paymentMethod: payment,
        subtotal,
        shippingCost: shipping,
        discount: totalDiscount,
        couponCode: appliedCoupon,
        total,
        items: lines.map((l) => ({ name: l.name, price: l.price, qty: l.qty })),
      });

      if (loyaltyContact.trim()) {
        awardOrderPoints(orderId, loyaltyContact.trim()).catch(() => {});
      }

      if (payment === "card") {
        setRedirecting(true);
        const res = await fetch("/api/checkout/create-session", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({
            orderId,
            items: lines.map((l) => ({ name: l.name, price: l.price, qty: l.qty })),
            shippingCost: shipping,
            discount: totalDiscount,
          }),
        });
        const data = await res.json();
        if (!res.ok || !data.url) {
          throw new Error(data.error ?? "No se pudo iniciar el pago con tarjeta.");
        }
        window.location.href = data.url;
        return;
      }

      const orderNumber = Math.floor(1000 + Math.random() * 9000);
      setConfirmedOrder(orderNumber);
    } catch (err) {
      setRedirecting(false);
      setSubmitError(err instanceof Error ? err.message : "No se pudo registrar tu pedido. Intenta de nuevo.");
    } finally {
      setSubmitting(false);
    }
  }

  if (confirmedOrder) {
    const summary = lines.map((l) => `${l.qty}x ${l.name}`).join(", ");
    const message = `¡Hola! Confirmo mi pedido #${confirmedOrder} (${
      fulfillment === "pickup" ? "recoger en tienda" : "a domicilio"
    }): ${summary}. Total: ${formatMXN(total)}. Pago: ${payment === "card" ? "tarjeta" : "efectivo"}.`;

    return (
      <div className="glass-strong mx-auto max-w-lg rounded-3xl p-8 text-center">
        <span className="text-5xl">✅</span>
        <h2 className="mt-4 text-2xl font-bold">¡Pedido #{confirmedOrder} confirmado!</h2>
        <p className="mt-2 text-sm text-carrot-50/70">
          {fulfillment === "pickup"
            ? "Te esperamos en tienda para recoger tu pedido."
            : "Tu pedido va en camino a la dirección indicada."}{" "}
          Recibirás la confirmación final por WhatsApp.
        </p>
        <div className="mt-6 flex flex-col gap-3 sm:flex-row sm:justify-center">
          <a
            href={whatsappLink(message)}
            target="_blank"
            rel="noopener noreferrer"
            onClick={() => clearCart()}
            className="rounded-full bg-leaf-500 px-6 py-3 text-sm font-semibold text-ink-950 hover:bg-leaf-400"
          >
            Confirmar por WhatsApp
          </a>
          <a
            href="/menu"
            onClick={() => clearCart()}
            className="glass glass-card rounded-full px-6 py-3 text-sm font-semibold text-carrot-50"
          >
            Seguir explorando el menú
          </a>
        </div>
      </div>
    );
  }

  if (displayLines.length === 0) {
    return (
      <div className="glass mx-auto max-w-lg rounded-3xl p-10 text-center">
        <ShoppingBasket className="mx-auto text-carrot-300" size={40} />
        <h2 className="mt-4 text-xl font-bold">Tu carrito está vacío</h2>
        <p className="mt-2 text-sm text-carrot-50/70">Agrega platillos desde el menú para comenzar tu pedido.</p>
        <a
          href="/menu"
          className="mt-6 inline-block rounded-full bg-carrot-500 px-6 py-3 text-sm font-semibold text-ink-950 hover:bg-carrot-400"
        >
          Ir al menú
        </a>
      </div>
    );
  }

  return (
    <div className="grid gap-6 lg:grid-cols-[1.4fr_1fr]">
      <div className="glass rounded-2xl p-5">
        <h2 className="mb-4 text-lg font-semibold">Tu pedido</h2>
        <ul className="space-y-3">
          {lines.map((line) => (
            <li key={line.id} className="flex items-center gap-3 rounded-xl bg-carrot-50/5 p-3">
              <span className="text-2xl">{line.emoji}</span>
              <div className="flex-1">
                <p className="text-sm font-medium">{line.name}</p>
                <p className="text-xs text-carrot-50/60">{formatMXN(line.price)} c/u</p>
              </div>
              <div className="glass flex items-center gap-2 rounded-full px-2 py-1">
                <button
                  type="button"
                  aria-label="Quitar uno"
                  onClick={() => setQty(line.id, line.qty - 1)}
                  className="grid h-6 w-6 place-items-center rounded-full bg-carrot-50/10 hover:bg-carrot-50/20"
                >
                  <Minus size={12} />
                </button>
                <span className="w-4 text-center text-xs font-semibold">{line.qty}</span>
                <button
                  type="button"
                  aria-label="Agregar uno"
                  onClick={() => setQty(line.id, line.qty + 1)}
                  className="grid h-6 w-6 place-items-center rounded-full bg-carrot-500 text-ink-950 hover:bg-carrot-400"
                >
                  <Plus size={12} />
                </button>
              </div>
              <button
                type="button"
                aria-label="Eliminar"
                onClick={() => removeFromCart(line.id)}
                className="text-carrot-50/40 hover:text-red-400"
              >
                <Trash2 size={16} />
              </button>
            </li>
          ))}
        </ul>

        <div className="mt-6">
          <p className="mb-2 text-sm font-semibold">Entrega</p>
          <div className="grid grid-cols-2 gap-2">
            {(["pickup", "delivery"] as Fulfillment[]).map((opt) => (
              <button
                key={opt}
                type="button"
                onClick={() => setFulfillment(opt)}
                className={cn(
                  "rounded-xl px-4 py-2.5 text-sm font-medium transition",
                  fulfillment === opt ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75 hover:bg-carrot-50/20"
                )}
              >
                {opt === "pickup" ? "🏬 Recoger en tienda" : "🚚 A domicilio"}
              </button>
            ))}
          </div>
          {fulfillment === "delivery" && (
            <div className="mt-3">
              <input
                type="text"
                value={address}
                onChange={(e) => setAddress(e.target.value)}
                placeholder="Dirección de entrega en Ciudad Victoria"
                className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
              />
              <p className="mt-1.5 text-xs text-carrot-50/50">
                Envío gratis en pedidos desde {formatMXN(SITE.shipping.freeThreshold)}.
              </p>
            </div>
          )}
        </div>

        <div className="mt-6">
          <p className="mb-2 text-sm font-semibold">Método de pago</p>
          <div className="grid grid-cols-2 gap-2">
            {(["card", "cash"] as Payment[]).map((opt) => (
              <button
                key={opt}
                type="button"
                onClick={() => setPayment(opt)}
                className={cn(
                  "rounded-xl px-4 py-2.5 text-sm font-medium transition",
                  payment === opt ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75 hover:bg-carrot-50/20"
                )}
              >
                {opt === "card" ? "💳 Tarjeta" : "💵 Efectivo"}
              </button>
            ))}
          </div>
        </div>

        <div className="mt-6">
          <p className="mb-2 text-sm font-semibold">Fidelización (opcional)</p>
          <input
            type="text"
            value={loyaltyContact}
            onChange={(e) => setLoyaltyContact(e.target.value)}
            onBlur={(e) => lookupSubscriberPlan(e.target.value)}
            placeholder="Tu correo o teléfono registrado"
            className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
          />
          <p className="mt-1.5 text-xs text-carrot-50/50">Gana 10% de puntos sobre tu compra en tu monedero.</p>
          {subscriptionDiscount && (
            <p className="mt-1.5 text-xs text-leaf-300">🥕 Suscripción activa: {subscriptionDiscount.label} ✓</p>
          )}
        </div>
      </div>

      <div className="glass-strong h-fit rounded-2xl p-5">
        <h2 className="mb-4 text-lg font-semibold">Resumen</h2>

        <div className="flex gap-2">
          <input
            type="text"
            value={couponInput}
            onChange={(e) => setCouponInput(e.target.value)}
            placeholder="Código de cupón"
            className="flex-1 rounded-xl bg-carrot-50/10 px-3 py-2 text-sm uppercase placeholder:normal-case placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
          />
          <button
            type="button"
            onClick={applyCoupon}
            className="rounded-xl bg-carrot-50/15 px-4 text-sm font-semibold hover:bg-carrot-50/25"
          >
            Aplicar
          </button>
        </div>
        {couponError && <p className="mt-1.5 text-xs text-red-400">{couponError}</p>}
        {appliedCoupon && <p className="mt-1.5 text-xs text-leaf-300">Cupón {appliedCoupon} aplicado ✓</p>}
        {couponNote && <p className="mt-1 text-xs text-carrot-50/50">{couponNote}</p>}

        <dl className="mt-5 space-y-2 text-sm">
          <div className="flex justify-between text-carrot-50/70">
            <dt>Subtotal</dt>
            <dd>{formatMXN(subtotal)}</dd>
          </div>
          <div className="flex justify-between text-carrot-50/70">
            <dt>Envío</dt>
            <dd>{shipping === 0 ? "Gratis" : formatMXN(shipping)}</dd>
          </div>
          {discount > 0 && (
            <div className="flex justify-between text-leaf-300">
              <dt>Descuento</dt>
              <dd>-{formatMXN(discount)}</dd>
            </div>
          )}
          {subscriptionDiscount && (
            <div className="flex justify-between text-leaf-300">
              <dt>{subscriptionDiscount.label}</dt>
              <dd>-{formatMXN(subscriptionDiscount.amount)}</dd>
            </div>
          )}
          <div className="flex justify-between border-t border-carrot-50/15 pt-3 text-base font-bold">
            <dt>Total</dt>
            <dd>{formatMXN(total)}</dd>
          </div>
        </dl>

        {submitError && <p className="mt-3 text-xs text-red-400">{submitError}</p>}

        <button
          type="button"
          onClick={confirmOrder}
          disabled={submitting || redirecting || (fulfillment === "delivery" && address.trim().length < 6)}
          className="mt-6 w-full rounded-full bg-carrot-500 py-3 text-sm font-semibold text-ink-950 transition hover:bg-carrot-400 disabled:cursor-not-allowed disabled:opacity-40"
        >
          {redirecting
            ? "Redirigiendo a pago seguro..."
            : submitting
              ? "Enviando..."
              : payment === "card"
                ? "Ir a pagar con tarjeta"
                : "Confirmar pedido"}
        </button>
      </div>
    </div>
  );
}
</file>

<file path="src/components/islands/CategoriesGrid.tsx">
import { fetchCategories } from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";

export default function CategoriesGrid() {
  const { data: categories, error, loading } = useSupabaseData(fetchCategories);

  if (loading) return <p className="mt-8 text-sm text-carrot-50/60">Cargando categorías...</p>;
  if (error) return <p className="mt-8 text-sm text-red-400">{error}</p>;

  return (
    <div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
      {categories?.map((c) => (
        <a
          key={c.slug}
          href={`/menu?categoria=${c.slug}`}
          className="glass glass-card flex flex-col items-center rounded-2xl p-6 text-center"
        >
          <span className="text-4xl">{c.emoji}</span>
          <h3 className="mt-3 font-semibold">{c.name}</h3>
        </a>
      ))}
    </div>
  );
}
</file>

<file path="src/components/islands/EmployeePortal.tsx">
import { useEffect, useState } from "react";
import type { JobPosition } from "@/data/team";
import {
  fetchJobPositions,
  fetchTrainingModules,
  fetchTrainingProgress,
  markTrainingComplete,
  submitEmployeeReferral,
  submitJobApplication,
  uploadJobApplicationCv,
} from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";
import { useStaffSession } from "@/hooks/useStaffSession";
import StaffLogin from "@/components/islands/StaffLogin";
import { cn } from "@/lib/utils";

type Tab = "capacitacion" | "manuales" | "referidos" | "bolsa";

function PositionSelect({
  positions,
  value,
  onChange,
}: {
  positions: JobPosition[];
  value: string;
  onChange: (id: string) => void;
}) {
  return (
    <select
      value={value}
      onChange={(e) => onChange(e.target.value)}
      className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
    >
      {positions.map((p) => (
        <option className="bg-ink-900" key={p.id} value={p.id}>
          {p.title}
        </option>
      ))}
    </select>
  );
}

function ReferralForm({ positions }: { positions: JobPosition[] }) {
  const [candidate, setCandidate] = useState("");
  const [phone, setPhone] = useState("");
  const [position, setPosition] = useState("");
  const [sent, setSent] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState("");

  useEffect(() => {
    if (!position && positions[0]) setPosition(positions[0].id);
  }, [positions, position]);

  if (sent) {
    return (
      <div className="glass rounded-2xl p-6 text-center">
        <span className="text-3xl">🤝</span>
        <p className="mt-2 font-semibold">¡Recomendación registrada!</p>
        <p className="mt-1 text-sm text-carrot-50/65">
          Si tu candidato cumple 3 meses, tendrás 7 días para reclamar tu bono de recomendación.
        </p>
      </div>
    );
  }

  return (
    <form
      className="glass space-y-4 rounded-2xl p-6"
      onSubmit={async (e) => {
        e.preventDefault();
        if (!candidate.trim() || !phone.trim() || !position) return;
        setSubmitting(true);
        setSubmitError("");
        try {
          await submitEmployeeReferral({
            candidateName: candidate.trim(),
            candidatePhone: phone.trim(),
            positionId: position,
          });
          setSent(true);
        } catch (err) {
          setSubmitError(err instanceof Error ? err.message : "No se pudo enviar la recomendación.");
        } finally {
          setSubmitting(false);
        }
      }}
    >
      <p className="text-sm text-carrot-50/70">
        Recomienda a alguien y recibe un bono cuando cumpla 3 meses trabajando con nosotros. Tienes 7 días después
        de esa fecha para reclamarlo.
      </p>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Nombre del candidato</label>
        <input
          type="text"
          value={candidate}
          onChange={(e) => setCandidate(e.target.value)}
          required
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
        />
      </div>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Teléfono del candidato</label>
        <input
          type="tel"
          value={phone}
          onChange={(e) => setPhone(e.target.value)}
          required
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
        />
      </div>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Puesto sugerido</label>
        <PositionSelect positions={positions} value={position} onChange={setPosition} />
      </div>
      {submitError && <p className="text-sm text-red-400">{submitError}</p>}
      <button
        type="submit"
        disabled={submitting}
        className="w-full rounded-full bg-carrot-500 py-2.5 text-sm font-semibold text-ink-950 hover:bg-carrot-400 disabled:cursor-not-allowed disabled:opacity-40"
      >
        {submitting ? "Enviando..." : "Enviar recomendación"}
      </button>
    </form>
  );
}

function JobApplicationForm({ positions }: { positions: JobPosition[] }) {
  const [name, setName] = useState("");
  const [phone, setPhone] = useState("");
  const [position, setPosition] = useState("");
  const [file, setFile] = useState<File | null>(null);
  const [sent, setSent] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState("");

  useEffect(() => {
    if (!position && positions[0]) setPosition(positions[0].id);
  }, [positions, position]);

  if (sent) {
    return (
      <div className="glass rounded-2xl p-6 text-center">
        <span className="text-3xl">📄</span>
        <p className="mt-2 font-semibold">¡Solicitud enviada!</p>
        <p className="mt-1 text-sm text-carrot-50/65">Nuestro equipo de RH revisará tu perfil y te contactará.</p>
      </div>
    );
  }

  return (
    <form
      className="glass space-y-4 rounded-2xl p-6"
      onSubmit={async (e) => {
        e.preventDefault();
        if (!name.trim() || !phone.trim() || !position) return;
        setSubmitting(true);
        setSubmitError("");
        try {
          const cvPath = file ? await uploadJobApplicationCv(file) : null;
          await submitJobApplication({ name: name.trim(), phone: phone.trim(), positionId: position, cvPath });
          setSent(true);
        } catch (err) {
          setSubmitError(err instanceof Error ? err.message : "No se pudo enviar tu solicitud.");
        } finally {
          setSubmitting(false);
        }
      }}
    >
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Nombre completo</label>
        <input
          type="text"
          value={name}
          onChange={(e) => setName(e.target.value)}
          required
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
        />
      </div>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Teléfono de contacto</label>
        <input
          type="tel"
          value={phone}
          onChange={(e) => setPhone(e.target.value)}
          required
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
        />
      </div>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">Puesto de interés</label>
        <PositionSelect positions={positions} value={position} onChange={setPosition} />
      </div>
      <div>
        <label className="mb-1.5 block text-sm font-semibold">CV o documentos</label>
        <input
          type="file"
          accept=".pdf,.doc,.docx,image/*"
          onChange={(e) => setFile(e.target.files?.[0] ?? null)}
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm file:mr-3 file:rounded-full file:border-0 file:bg-carrot-500 file:px-3 file:py-1.5 file:text-xs file:font-semibold file:text-ink-950"
        />
        {file && <p className="mt-1.5 text-xs text-carrot-50/50">{file.name} seleccionado.</p>}
      </div>
      {submitError && <p className="text-sm text-red-400">{submitError}</p>}
      <button
        type="submit"
        disabled={submitting}
        className="w-full rounded-full bg-carrot-500 py-2.5 text-sm font-semibold text-ink-950 hover:bg-carrot-400 disabled:cursor-not-allowed disabled:opacity-40"
      >
        {submitting ? "Enviando..." : "Enviar solicitud"}
      </button>
    </form>
  );
}

export default function EmployeePortal() {
  const { employee, loading: sessionLoading, login, logout } = useStaffSession();
  const [tab, setTab] = useState<Tab>("capacitacion");
  const { data: positions, error: positionsError, loading: positionsLoading } = useSupabaseData(fetchJobPositions);
  const [modules, setModules] = useState<Awaited<ReturnType<typeof fetchTrainingModules>> | null>(null);
  const [modulesError, setModulesError] = useState("");
  const [progress, setProgress] = useState<string[]>([]);

  // Modules and progress are RLS-gated to logged-in employees, so they can only be
  // fetched once the Supabase Auth session is actually established (not on first mount).
  useEffect(() => {
    if (!employee) return;
    fetchTrainingModules()
      .then(setModules)
      .catch((err) => setModulesError(err instanceof Error ? err.message : "No se pudieron cargar los módulos."));
    fetchTrainingProgress(employee.id).then(setProgress).catch(() => {});
  }, [employee]);

  if (sessionLoading) return <p className="text-sm text-carrot-50/60">Cargando sesión...</p>;
  if (!employee) return <StaffLogin onLogin={login} />;

  async function completeModule(moduleId: string) {
    if (!employee) return;
    try {
      await markTrainingComplete(employee.id, moduleId);
      setProgress((p) => [...p, moduleId]);
    } catch {
      /* ya estaba marcado o RLS lo rechazó */
    }
  }

  const tabs: { id: Tab; label: string }[] = [
    { id: "capacitacion", label: "Capacitación" },
    { id: "manuales", label: "Manuales y puestos" },
    { id: "referidos", label: "Recomendar candidato" },
    { id: "bolsa", label: "Bolsa de trabajo" },
  ];

  return (
    <div>
      <div className="mb-4 flex flex-wrap items-center justify-between gap-3">
        <p className="text-sm text-carrot-50/70">
          Sesión de <span className="font-semibold text-carrot-50">{employee.name}</span>{" "}
          <span className="rounded-full bg-carrot-50/10 px-2 py-0.5 text-[10px] font-semibold uppercase tracking-wider text-carrot-300">
            {employee.role === "admin" ? "administrador" : "personal"}
          </span>
        </p>
        <div className="flex items-center gap-2">
          {employee.role === "admin" && (
            <a
              href="/admin"
              className="rounded-full bg-carrot-50/10 px-4 py-2 text-xs font-semibold text-carrot-50/80 hover:bg-carrot-50/20"
            >
              Panel administrativo →
            </a>
          )}
          <button
            type="button"
            onClick={logout}
            className="glass glass-card rounded-full px-4 py-2 text-xs font-semibold text-carrot-50/80"
          >
            Cerrar sesión
          </button>
        </div>
      </div>

      <div className="glass mb-6 flex flex-wrap gap-2 rounded-2xl p-2">
        {tabs.map((t) => (
          <button
            key={t.id}
            type="button"
            onClick={() => setTab(t.id)}
            className={cn(
              "rounded-xl px-4 py-2 text-sm font-medium transition",
              tab === t.id ? "bg-carrot-500 text-ink-950" : "text-carrot-50/70 hover:bg-carrot-50/10"
            )}
          >
            {t.label}
          </button>
        ))}
      </div>

      {tab === "capacitacion" &&
        (modules === null && !modulesError ? (
          <p className="text-sm text-carrot-50/60">Cargando módulos...</p>
        ) : modulesError ? (
          <p className="text-sm text-red-400">{modulesError}</p>
        ) : (
          <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
            {modules?.map((m) => {
              const done = progress.includes(m.id);
              return (
                <div key={m.id} className="glass glass-card rounded-2xl p-5">
                  <div className="flex items-center justify-between">
                    <span className="rounded-full bg-carrot-50/10 px-2 py-1 text-[10px] font-semibold text-carrot-50/70">
                      {m.type}
                    </span>
                    {done && <span className="text-xs font-semibold text-leaf-300">Completado ✓</span>}
                  </div>
                  <h3 className="mt-3 font-semibold">{m.title}</h3>
                  <p className="mt-1 text-xs text-carrot-50/50">{m.duration}</p>
                  <p className="mt-2 text-xs text-carrot-50/40">
                    {m.contentUrl ? "Contenido disponible" : "Contenido pendiente de subir por el equipo"}
                  </p>
                  {!done && (
                    <button
                      type="button"
                      onClick={() => completeModule(m.id)}
                      className="mt-3 w-full rounded-full bg-carrot-50/10 py-2 text-xs font-semibold hover:bg-carrot-50/20"
                    >
                      Marcar como completado
                    </button>
                  )}
                </div>
              );
            })}
          </div>
        ))}

      {tab === "manuales" &&
        (positionsLoading ? (
          <p className="text-sm text-carrot-50/60">Cargando puestos...</p>
        ) : positionsError ? (
          <p className="text-sm text-red-400">{positionsError}</p>
        ) : (
          <div className="grid gap-4 sm:grid-cols-2">
            {positions?.map((p) => (
              <div key={p.id} className="glass rounded-2xl p-5">
                <h3 className="font-semibold">{p.title}</h3>
                <p className="mt-1 text-sm text-carrot-50/65">{p.summary}</p>
                <ul className="mt-3 space-y-1.5 text-xs text-carrot-50/60">
                  {p.responsibilities.map((r) => (
                    <li key={r}>• {r}</li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        ))}

      {tab === "referidos" && (positions ? <ReferralForm positions={positions} /> : <p className="text-sm text-carrot-50/60">Cargando puestos...</p>)}
      {tab === "bolsa" && (positions ? <JobApplicationForm positions={positions} /> : <p className="text-sm text-carrot-50/60">Cargando puestos...</p>)}
    </div>
  );
}
</file>

<file path="src/components/islands/GoogleReviewLink.tsx">
import { SITE } from "@/data/site";
import { submitGoogleReviewClick } from "@/lib/queries";
import { cn } from "@/lib/utils";

export default function GoogleReviewLink({ className }: { className?: string }) {
  return (
    <a
      href={SITE.googleReviewUrl}
      target="_blank"
      rel="noopener noreferrer"
      onClick={() => {
        submitGoogleReviewClick().catch(() => {});
      }}
      className={cn(
        "inline-block rounded-full bg-carrot-500 px-6 py-3 text-sm font-semibold text-ink-950 hover:bg-carrot-400",
        className
      )}
    >
      Escribir reseña en Google
    </a>
  );
}
</file>

<file path="src/components/islands/LoyaltyDashboard.tsx">
import { useEffect, useState } from "react";
import { useStore } from "@nanostores/react";
import { Copy, Gift, LogOut, Wallet } from "lucide-react";
import {
  $loyaltyProfile,
  $loyaltyLoading,
  logoutLoyalty,
  registerLoyalty,
  loginLoyalty,
  restoreLoyaltySession,
} from "@/stores/loyalty";
import { sendGiftCard } from "@/lib/queries";
import { whatsappLink } from "@/data/site";
import { formatMXN } from "@/lib/utils";
import QrPreview from "@/components/islands/QrPreview";

function GiftCardForm({ senderContact }: { senderContact: string }) {
  const [open, setOpen] = useState(false);
  const [recipient, setRecipient] = useState("");
  const [amount, setAmount] = useState("");
  const [sending, setSending] = useState(false);
  const [error, setError] = useState("");
  const [sent, setSent] = useState(false);

  if (!open) {
    return (
      <button
        type="button"
        onClick={() => setOpen(true)}
        className="mt-3 w-full rounded-full bg-carrot-50/10 py-2 text-xs font-semibold text-carrot-50/80 hover:bg-carrot-50/20"
      >
        Regalar gift card
      </button>
    );
  }

  if (sent) {
    return <p className="mt-3 text-xs text-leaf-300">¡Gift card enviada! ✓</p>;
  }

  return (
    <form
      className="mt-3 space-y-2"
      onSubmit={async (e) => {
        e.preventDefault();
        setSending(true);
        setError("");
        try {
          await sendGiftCard(senderContact, recipient.trim(), Number(amount));
          await restoreLoyaltySession();
          setSent(true);
          setTimeout(() => {
            setOpen(false);
            setSent(false);
            setRecipient("");
            setAmount("");
          }, 1800);
        } catch (err) {
          setError(err instanceof Error ? err.message : "No se pudo enviar la gift card.");
        } finally {
          setSending(false);
        }
      }}
    >
      <input
        type="text"
        required
        value={recipient}
        onChange={(e) => setRecipient(e.target.value)}
        placeholder="Correo o teléfono del destinatario"
        className="w-full rounded-lg bg-carrot-50/10 px-3 py-2 text-xs placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
      />
      <input
        type="number"
        required
        min={1}
        value={amount}
        onChange={(e) => setAmount(e.target.value)}
        placeholder="Monto"
        className="w-full rounded-lg bg-carrot-50/10 px-3 py-2 text-xs placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
      />
      {error && <p className="text-xs text-red-400">{error}</p>}
      <button
        type="submit"
        disabled={sending}
        className="w-full rounded-full bg-carrot-500 py-2 text-xs font-semibold text-ink-950 hover:bg-carrot-400 disabled:opacity-40"
      >
        {sending ? "Enviando..." : "Enviar gift card"}
      </button>
    </form>
  );
}

function AuthPanel() {
  const [mode, setMode] = useState<"register" | "login">("register");
  const [name, setName] = useState("");
  const [contact, setContact] = useState("");
  const [referralCode, setReferralCode] = useState("");
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState("");

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setSubmitting(true);
    setError("");
    try {
      if (mode === "register") {
        if (!name.trim() || !contact.trim()) return;
        await registerLoyalty(name.trim(), contact.trim(), referralCode.trim() || undefined);
      } else {
        if (!contact.trim()) return;
        await loginLoyalty(contact.trim());
      }
    } catch (err) {
      setError(err instanceof Error ? err.message : "Algo salió mal. Intenta de nuevo.");
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <div className="glass mx-auto max-w-md rounded-2xl p-6">
      <div className="mb-5 grid grid-cols-2 gap-2 rounded-full bg-carrot-50/10 p-1 text-xs font-semibold">
        <button
          type="button"
          onClick={() => setMode("register")}
          className={`rounded-full py-2 transition ${mode === "register" ? "bg-carrot-500 text-ink-950" : "text-carrot-50/70"}`}
        >
          Crear cuenta
        </button>
        <button
          type="button"
          onClick={() => setMode("login")}
          className={`rounded-full py-2 transition ${mode === "login" ? "bg-carrot-500 text-ink-950" : "text-carrot-50/70"}`}
        >
          Ya tengo cuenta
        </button>
      </div>

      <form className="space-y-4" onSubmit={handleSubmit}>
        <div>
          <span className="text-3xl">💳</span>
          <h2 className="mt-2 text-lg font-bold">
            {mode === "register" ? "Únete al programa de fidelización" : "Accede a tu monedero"}
          </h2>
          <p className="mt-1 text-sm text-carrot-50/65">
            {mode === "register"
              ? "Regístrate con tu correo o teléfono y obtén 150 puntos de bienvenida más $50 en tu monedero."
              : "Ingresa el correo o teléfono con el que te registraste."}
          </p>
        </div>

        {mode === "register" && (
          <div>
            <label className="mb-1.5 block text-sm font-semibold">Nombre completo</label>
            <input
              type="text"
              value={name}
              onChange={(e) => setName(e.target.value)}
              required
              className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
            />
          </div>
        )}

        <div>
          <label className="mb-1.5 block text-sm font-semibold">Correo o número telefónico</label>
          <input
            type="text"
            value={contact}
            onChange={(e) => setContact(e.target.value)}
            required
            placeholder="tu@correo.com o 834 123 4567"
            className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
          />
        </div>

        {mode === "register" && (
          <div>
            <label className="mb-1.5 block text-sm font-semibold">Código de referido (opcional)</label>
            <input
              type="text"
              value={referralCode}
              onChange={(e) => setReferralCode(e.target.value)}
              placeholder="AMIGO1234"
              className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm uppercase placeholder:normal-case placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
            />
          </div>
        )}

        {error && <p className="text-xs text-red-400">{error}</p>}

        <button
          type="submit"
          disabled={submitting}
          className="w-full rounded-full bg-carrot-500 py-3 text-sm font-semibold text-ink-950 transition hover:bg-carrot-400 disabled:cursor-not-allowed disabled:opacity-40"
        >
          {submitting ? "Enviando..." : mode === "register" ? "Crear mi cuenta" : "Entrar"}
        </button>
      </form>
    </div>
  );
}

export default function LoyaltyDashboard() {
  const profile = useStore($loyaltyProfile);
  const loading = useStore($loyaltyLoading);
  const [copied, setCopied] = useState(false);

  useEffect(() => {
    restoreLoyaltySession();
  }, []);

  // Only show the full-screen loader for the *first* load. Background
  // refreshes (e.g. GiftCardForm calling restoreLoyaltySession() after a
  // transfer) also flip `loading`, and unmounting the whole dashboard for
  // those discards in-flight local state in children like GiftCardForm —
  // its confirmation message would fire on an already-unmounted instance.
  if (loading && !profile) {
    return <p className="mt-8 text-center text-sm text-carrot-50/60">Cargando tu cuenta...</p>;
  }

  if (!profile) return <AuthPanel />;

  function copyReferral() {
    if (!profile) return;
    navigator.clipboard?.writeText(profile.referralCode).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    });
  }

  return (
    <div className="space-y-6">
      <div className="flex flex-col items-start justify-between gap-4 sm:flex-row sm:items-center">
        <div>
          <p className="text-sm text-carrot-50/60">Hola de nuevo,</p>
          <h2 className="text-2xl font-bold">{profile.name}</h2>
          <p className="text-xs text-carrot-50/50">Miembro desde {new Date(profile.joinedAt).toLocaleDateString("es-MX")}</p>
        </div>
        <button
          type="button"
          onClick={logoutLoyalty}
          className="glass glass-card flex items-center gap-2 rounded-full px-4 py-2 text-xs font-semibold text-carrot-50/80"
        >
          <LogOut size={14} /> Cerrar sesión
        </button>
      </div>

      <div className="grid gap-5 lg:grid-cols-3">
        <div className="glass-strong rounded-2xl p-6">
          <p className="text-xs font-semibold uppercase tracking-wider text-carrot-300">Puntos acumulados</p>
          <p className="mt-2 text-4xl font-bold">{profile.points}</p>
          <p className="mt-1 text-xs text-carrot-50/60">10% de cada compra regresa en puntos</p>
        </div>
        <div className="glass-strong rounded-2xl p-6">
          <p className="flex items-center gap-1.5 text-xs font-semibold uppercase tracking-wider text-carrot-300">
            <Wallet size={13} /> Monedero electrónico
          </p>
          <p className="mt-2 text-4xl font-bold">{formatMXN(profile.walletBalance)}</p>
          <p className="mt-1 text-xs text-carrot-50/60">Úsalo como pago en tu próxima compra</p>
        </div>
        <div className="glass-strong rounded-2xl p-6">
          <p className="flex items-center gap-1.5 text-xs font-semibold uppercase tracking-wider text-carrot-300">
            <Gift size={13} /> Gift cards
          </p>
          <p className="mt-2 text-4xl font-bold">{formatMXN(profile.giftCardBalance)}</p>
          <p className="mt-1 text-xs text-carrot-50/60">Envía saldo de tu monedero como gift card</p>
          <GiftCardForm senderContact={profile.email ?? profile.phone ?? ""} />
        </div>
      </div>

      <div className="grid gap-5 lg:grid-cols-[1fr_1.4fr]">
        <div className="glass flex flex-col items-center rounded-2xl p-6 text-center">
          <p className="mb-3 text-sm font-semibold">Tu código QR de cliente</p>
          <QrPreview seed={profile.memberCode} />
          <p className="mt-3 font-mono text-xs text-carrot-50/60">{profile.memberCode}</p>
          <p className="mt-1 text-xs text-carrot-50/50">Muéstralo en caja para acumular y canjear puntos al instante.</p>
        </div>

        <div className="glass rounded-2xl p-6">
          <p className="mb-3 text-sm font-semibold">Historial de compras</p>
          {profile.purchaseHistory.length === 0 ? (
            <p className="text-sm text-carrot-50/50">Aún no tienes compras registradas con tu cuenta de fidelización.</p>
          ) : (
            <ul className="space-y-2">
              {profile.purchaseHistory.map((h) => (
                <li key={h.orderId} className="flex items-center justify-between rounded-xl bg-carrot-50/5 p-3 text-sm">
                  <div>
                    <p className="font-medium">Pedido #{h.orderId.slice(0, 8)}</p>
                    <p className="text-xs text-carrot-50/50">{new Date(h.date).toLocaleDateString("es-MX")}</p>
                  </div>
                  <div className="text-right">
                    <p className="font-semibold">{formatMXN(h.total)}</p>
                    <p className="text-xs text-leaf-300">+{h.pointsEarned} pts</p>
                  </div>
                </li>
              ))}
            </ul>
          )}
        </div>
      </div>

      <div className="glass rounded-2xl p-6">
        <p className="text-sm font-semibold">Recomienda y gana</p>
        <p className="mt-1 text-sm text-carrot-50/65">
          Comparte tu código y recibe $100 en tu monedero cuando tu amigo haga su primera compra.
        </p>
        <div className="mt-4 flex flex-col gap-2 sm:flex-row">
          <div className="glass flex flex-1 items-center justify-between rounded-xl px-4 py-2.5 font-mono text-sm">
            {profile.referralCode}
            <button type="button" onClick={copyReferral} className="text-carrot-300 hover:text-carrot-200">
              <Copy size={15} />
            </button>
          </div>
          <a
            href={whatsappLink(`¡Únete a Don Zanahorio con mi código ${profile.referralCode} y gana beneficios!`)}
            target="_blank"
            rel="noopener noreferrer"
            className="rounded-xl bg-leaf-500 px-5 py-2.5 text-center text-sm font-semibold text-ink-950 hover:bg-leaf-400"
          >
            Compartir por WhatsApp
          </a>
        </div>
        {copied && <p className="mt-2 text-xs text-leaf-300">Código copiado ✓</p>}
      </div>
    </div>
  );
}
</file>

<file path="src/components/islands/MayoreoForm.tsx">
import { useMemo, useState } from "react";
import { discountFor, WHOLESALE_ITEMS } from "@/data/wholesale";
import { SITE, whatsappLink } from "@/data/site";
import { submitWholesaleOrder } from "@/lib/queries";
import { cn, formatMXN } from "@/lib/utils";

type Fulfillment = "pickup" | "delivery";

export default function MayoreoForm() {
  const [itemId, setItemId] = useState(WHOLESALE_ITEMS[0].id);
  const [units, setUnits] = useState(WHOLESALE_ITEMS[0].minUnits);
  const [fulfillment, setFulfillment] = useState<Fulfillment>("pickup");
  const [name, setName] = useState("");
  const [company, setCompany] = useState("");
  const [phone, setPhone] = useState("");
  const [eventDate, setEventDate] = useState("");
  const [notes, setNotes] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState("");

  const item = WHOLESALE_ITEMS.find((i) => i.id === itemId)!;

  const quote = useMemo(() => {
    const discount = discountFor(units);
    const subtotal = units * item.pricePerUnit;
    const discountAmount = Math.round(subtotal * discount);
    const shipping = fulfillment === "delivery" ? SITE.shipping.baseCost * 2 : 0;
    const total = subtotal - discountAmount + shipping;
    return { discount, subtotal, discountAmount, shipping, total };
  }, [units, item, fulfillment]);

  const canSubmit = name.trim().length > 1 && phone.trim().length >= 10 && units >= item.minUnits;

  if (submitted) {
    const message = `¡Hola! Soy ${name}${company ? ` de ${company}` : ""}. Quiero cotizar ${units} ${item.unit}(s) de "${item.name}" para ${
      fulfillment === "pickup" ? "recoger en tienda" : "entrega a domicilio"
    }${eventDate ? `, fecha: ${eventDate}` : ""}. Cotización estimada: ${formatMXN(quote.total)}.`;

    return (
      <div className="glass-strong mx-auto max-w-lg rounded-3xl p-8 text-center">
        <span className="text-5xl">📦</span>
        <h2 className="mt-4 text-2xl font-bold">¡Solicitud recibida!</h2>
        <p className="mt-2 text-sm text-carrot-50/70">
          Nuestro equipo confirmará disponibilidad y te enviará la cotización final.
        </p>
        <a
          href={whatsappLink(message)}
          target="_blank"
          rel="noopener noreferrer"
          className="mt-6 inline-block rounded-full bg-leaf-500 px-6 py-3 text-sm font-semibold text-ink-950 hover:bg-leaf-400"
        >
          Enviar detalles por WhatsApp
        </a>
      </div>
    );
  }

  return (
    <div className="grid gap-6 lg:grid-cols-[1.4fr_1fr]">
      <form
        className="glass space-y-5 rounded-2xl p-6"
        onSubmit={async (e) => {
          e.preventDefault();
          if (!canSubmit) return;
          setSubmitting(true);
          setSubmitError("");
          try {
            await submitWholesaleOrder({
              customerName: name.trim(),
              company: company.trim() || null,
              phone: phone.trim(),
              itemName: item.name,
              units,
              unitPrice: item.pricePerUnit,
              fulfillmentType: fulfillment,
              eventDate: eventDate || null,
              notes: notes.trim() || null,
              quoteTotal: quote.total,
            });
            setSubmitted(true);
          } catch (err) {
            setSubmitError(err instanceof Error ? err.message : "No se pudo enviar tu solicitud. Intenta de nuevo.");
          } finally {
            setSubmitting(false);
          }
        }}
      >
        <div>
          <label className="mb-1.5 block text-sm font-semibold">Producto</label>
          <select
            value={itemId}
            onChange={(e) => {
              const next = WHOLESALE_ITEMS.find((i) => i.id === e.target.value)!;
              setItemId(next.id);
              setUnits(next.minUnits);
            }}
            className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
          >
            {WHOLESALE_ITEMS.map((i) => (
              <option className="bg-ink-900" key={i.id} value={i.id}>
                {i.name} — {formatMXN(i.pricePerUnit)}/{i.unit}
              </option>
            ))}
          </select>
        </div>

        <div>
          <label className="mb-1.5 block text-sm font-semibold">
            Cantidad ({item.unit}s, mínimo {item.minUnits})
          </label>
          <input
            type="number"
            min={item.minUnits}
            value={units}
            onChange={(e) => setUnits(Math.max(item.minUnits, Number(e.target.value) || item.minUnits))}
            className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
          />
        </div>

        <div className="grid grid-cols-2 gap-2">
          {(["pickup", "delivery"] as Fulfillment[]).map((opt) => (
            <button
              key={opt}
              type="button"
              onClick={() => setFulfillment(opt)}
              className={cn(
                "rounded-xl px-4 py-2.5 text-sm font-medium transition",
                fulfillment === opt ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75 hover:bg-carrot-50/20"
              )}
            >
              {opt === "pickup" ? "🏬 Recoger en tienda" : "🚚 Entrega a domicilio"}
            </button>
          ))}
        </div>

        <div className="grid gap-4 sm:grid-cols-2">
          <div>
            <label className="mb-1.5 block text-sm font-semibold">Nombre completo</label>
            <input
              type="text"
              value={name}
              onChange={(e) => setName(e.target.value)}
              required
              className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
            />
          </div>
          <div>
            <label className="mb-1.5 block text-sm font-semibold">Empresa (opcional)</label>
            <input
              type="text"
              value={company}
              onChange={(e) => setCompany(e.target.value)}
              className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
            />
          </div>
          <div>
            <label className="mb-1.5 block text-sm font-semibold">Teléfono</label>
            <input
              type="tel"
              value={phone}
              onChange={(e) => setPhone(e.target.value)}
              required
              className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
            />
          </div>
          <div>
            <label className="mb-1.5 block text-sm font-semibold">Fecha del evento</label>
            <input
              type="date"
              value={eventDate}
              onChange={(e) => setEventDate(e.target.value)}
              className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
            />
          </div>
        </div>

        <div>
          <label className="mb-1.5 block text-sm font-semibold">Notas adicionales</label>
          <textarea
            value={notes}
            onChange={(e) => setNotes(e.target.value)}
            rows={3}
            className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-carrot-400"
          />
        </div>

        {submitError && <p className="text-sm text-red-400">{submitError}</p>}

        <button
          type="submit"
          disabled={!canSubmit || submitting}
          className="w-full rounded-full bg-carrot-500 py-3 text-sm font-semibold text-ink-950 transition hover:bg-carrot-400 disabled:cursor-not-allowed disabled:opacity-40"
        >
          {submitting ? "Enviando..." : "Solicitar cotización"}
        </button>
      </form>

      <div className="glass-strong h-fit rounded-2xl p-6">
        <h2 className="mb-4 text-lg font-semibold">Cotización estimada</h2>
        <dl className="space-y-2 text-sm">
          <div className="flex justify-between text-carrot-50/70">
            <dt>
              {units} × {formatMXN(item.pricePerUnit)}
            </dt>
            <dd>{formatMXN(quote.subtotal)}</dd>
          </div>
          {quote.discount > 0 && (
            <div className="flex justify-between text-leaf-300">
              <dt>Descuento por volumen ({Math.round(quote.discount * 100)}%)</dt>
              <dd>-{formatMXN(quote.discountAmount)}</dd>
            </div>
          )}
          <div className="flex justify-between text-carrot-50/70">
            <dt>Envío</dt>
            <dd>{quote.shipping === 0 ? "Incluido" : formatMXN(quote.shipping)}</dd>
          </div>
          <div className="flex justify-between border-t border-carrot-50/15 pt-3 text-base font-bold">
            <dt>Total estimado</dt>
            <dd>{formatMXN(quote.total)}</dd>
          </div>
        </dl>
        <p className="mt-4 text-xs text-carrot-50/50">
          Cotización sujeta a confirmación final. Descuentos por volumen: 5% desde 20 unidades, 10% desde 50, 15% desde 100.
        </p>
      </div>
    </div>
  );
}
</file>

<file path="src/components/islands/MenuCatalog.tsx">
import { useMemo, useState } from "react";
import { useStore } from "@nanostores/react";
import { Minus, Plus, Search } from "lucide-react";
import { getVariants, variantDisplayName, type Product } from "@/data/products";
import { $cartRaw, addToCart, setQty } from "@/stores/cart";
import { formatMXN, cn } from "@/lib/utils";
import { fetchCategories, fetchProducts } from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";

function useQty(id: string) {
  const raw = useStore($cartRaw);
  const entry = raw[id];
  if (!entry) return 0;
  try {
    return (JSON.parse(entry) as { qty: number }).qty ?? 0;
  } catch {
    return 0;
  }
}

function VariantRow({ product, variant, emoji }: { product: Product; variant: ReturnType<typeof getVariants>[number]; emoji: string }) {
  const qty = useQty(variant.id);

  return (
    <div className="flex items-center justify-between rounded-xl bg-carrot-50/5 px-3 py-2">
      <span className="text-xs text-carrot-50/70">
        {variant.sizeLabel ?? "Precio"} · <span className="font-semibold text-carrot-200">{formatMXN(variant.price)}</span>
      </span>
      {qty === 0 ? (
        <button
          type="button"
          onClick={() => addToCart({ id: variant.id, name: variantDisplayName(product, variant), price: variant.price, emoji }, 1)}
          className="rounded-full bg-carrot-500 px-3 py-1 text-xs font-semibold text-ink-950 transition hover:bg-carrot-400"
        >
          Agregar
        </button>
      ) : (
        <div className="glass flex items-center gap-2 rounded-full px-2 py-0.5">
          <button
            type="button"
            aria-label="Quitar uno"
            onClick={() => setQty(variant.id, qty - 1)}
            className="grid h-6 w-6 place-items-center rounded-full bg-carrot-50/10 hover:bg-carrot-50/20"
          >
            <Minus size={12} />
          </button>
          <span className="w-4 text-center text-xs font-semibold">{qty}</span>
          <button
            type="button"
            aria-label="Agregar uno"
            onClick={() => addToCart({ id: variant.id, name: variantDisplayName(product, variant), price: variant.price, emoji }, 1)}
            className="grid h-6 w-6 place-items-center rounded-full bg-carrot-500 text-ink-950 hover:bg-carrot-400"
          >
            <Plus size={12} />
          </button>
        </div>
      )}
    </div>
  );
}

function ProductCard({ product, emoji }: { product: Product; emoji: string }) {
  const variants = getVariants(product);

  return (
    <div className="glass glass-card flex flex-col rounded-2xl p-5">
      <span className="text-3xl">{emoji}</span>
      <h3 className="mt-3 font-semibold text-carrot-50">{product.name}</h3>
      <p className="mt-1 flex-1 text-xs text-carrot-50/65">{product.description}</p>
      <div className="mt-4 space-y-2">
        {variants.map((variant) => (
          <VariantRow key={variant.id} product={product} variant={variant} emoji={emoji} />
        ))}
      </div>
    </div>
  );
}

function initialCategory() {
  if (typeof window === "undefined") return "Todos";
  return new URLSearchParams(window.location.search).get("categoria") || "Todos";
}

export default function MenuCatalog() {
  const [category, setCategory] = useState<string>(initialCategory);
  const [query, setQuery] = useState("");
  const { data: categories, error: categoriesError, loading: categoriesLoading } = useSupabaseData(fetchCategories);
  const { data: products, error: productsError, loading: productsLoading } = useSupabaseData(fetchProducts);

  const emojiBySlug = useMemo(() => {
    const map: Record<string, string> = {};
    categories?.forEach((c) => (map[c.slug] = c.emoji));
    return map;
  }, [categories]);

  const filtered = useMemo(() => {
    return (products ?? []).filter((p) => {
      const matchesCategory = category === "Todos" || p.categorySlug === category;
      const matchesQuery = p.name.toLowerCase().includes(query.trim().toLowerCase());
      return matchesCategory && matchesQuery;
    });
  }, [products, category, query]);

  const loading = categoriesLoading || productsLoading;
  const error = categoriesError || productsError;

  return (
    <div>
      <div className="glass sticky top-20 z-20 flex flex-col gap-4 rounded-2xl p-4 sm:flex-row sm:items-center sm:justify-between">
        <div className="flex flex-wrap gap-2">
          <button
            type="button"
            onClick={() => setCategory("Todos")}
            className={cn(
              "rounded-full px-3 py-1.5 text-xs font-medium transition",
              category === "Todos" ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75 hover:bg-carrot-50/20"
            )}
          >
            Todos
          </button>
          {categories?.map((c) => (
            <button
              key={c.slug}
              type="button"
              onClick={() => setCategory(c.slug)}
              className={cn(
                "rounded-full px-3 py-1.5 text-xs font-medium transition",
                category === c.slug ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75 hover:bg-carrot-50/20"
              )}
            >
              {c.emoji} {c.name}
            </button>
          ))}
        </div>
        <div className="relative">
          <Search size={15} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-carrot-50/50" />
          <input
            type="text"
            value={query}
            onChange={(e) => setQuery(e.target.value)}
            placeholder="Buscar platillo..."
            className="w-full rounded-full bg-carrot-50/10 py-2 pl-9 pr-4 text-sm text-carrot-50 placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400 sm:w-56"
          />
        </div>
      </div>

      {loading ? (
        <p className="mt-10 text-center text-sm text-carrot-50/60">Cargando menú...</p>
      ) : error ? (
        <p className="mt-10 text-center text-sm text-red-400">{error}</p>
      ) : filtered.length === 0 ? (
        <p className="mt-10 text-center text-sm text-carrot-50/60">No encontramos platillos con ese criterio.</p>
      ) : (
        <div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
          {filtered.map((product) => (
            <ProductCard key={product.id} product={product} emoji={emojiBySlug[product.categorySlug] ?? "🍽️"} />
          ))}
        </div>
      )}
    </div>
  );
}
</file>

<file path="src/components/islands/PlanCard.tsx">
import { useState } from "react";
import { useStore } from "@nanostores/react";
import { Check } from "lucide-react";
import { $loyaltyProfile, activatePlan } from "@/stores/loyalty";
import type { Plan } from "@/data/plans";
import { cn, formatMXN } from "@/lib/utils";

export default function PlanCard({ plan }: { plan: Plan }) {
  const profile = useStore($loyaltyProfile);
  const isActive = profile?.activePlanId === plan.id;
  const [activating, setActivating] = useState(false);
  const [error, setError] = useState("");

  async function handleActivate() {
    setActivating(true);
    setError("");
    try {
      await activatePlan(plan.id);
    } catch (err) {
      setError(err instanceof Error ? err.message : "No se pudo activar el plan.");
    } finally {
      setActivating(false);
    }
  }

  return (
    <div
      className={cn(
        "glass glass-card flex flex-col rounded-2xl p-6",
        plan.highlight && "border-carrot-400/50 ring-1 ring-carrot-400/30"
      )}
    >
      {plan.highlight && (
        <span className="mb-3 w-fit rounded-full bg-carrot-500/20 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-carrot-300">
          Más popular
        </span>
      )}
      <span className="text-4xl">{plan.emoji}</span>
      <h3 className="mt-3 text-lg font-bold">{plan.name}</h3>
      <p className="text-sm text-carrot-50/60">{plan.perk}</p>
      <p className="mt-4 text-3xl font-bold">
        {formatMXN(plan.price)}
        <span className="text-sm font-normal text-carrot-50/50">/{plan.period}</span>
      </p>
      <ul className="mt-5 flex-1 space-y-2 text-sm text-carrot-50/75">
        {plan.benefits.map((b) => (
          <li key={b} className="flex items-start gap-2">
            <Check size={15} className="mt-0.5 shrink-0 text-leaf-300" />
            {b}
          </li>
        ))}
      </ul>

      {isActive ? (
        <div className="mt-6 rounded-full bg-leaf-500/20 py-2.5 text-center text-sm font-semibold text-leaf-300">
          Plan activo ✓
        </div>
      ) : profile ? (
        <>
          <button
            type="button"
            onClick={handleActivate}
            disabled={activating}
            className="mt-6 rounded-full bg-carrot-500 py-2.5 text-sm font-semibold text-ink-950 transition hover:bg-carrot-400 disabled:opacity-40"
          >
            {activating ? "Activando..." : "Activar suscripción"}
          </button>
          {error && <p className="mt-2 text-xs text-red-400">{error}</p>}
        </>
      ) : (
        <a
          href="/fidelizacion"
          className="mt-6 rounded-full bg-carrot-50/10 py-2.5 text-center text-sm font-semibold text-carrot-50/80 hover:bg-carrot-50/20"
        >
          Regístrate para suscribirte
        </a>
      )}
    </div>
  );
}
</file>

<file path="src/components/islands/PlansList.tsx">
import { useEffect } from "react";
import { fetchPlans } from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";
import { restoreLoyaltySession } from "@/stores/loyalty";
import PlanCard from "@/components/islands/PlanCard";

export default function PlansList() {
  const { data: plans, error, loading } = useSupabaseData(fetchPlans);

  useEffect(() => {
    restoreLoyaltySession();
  }, []);

  if (loading) return <p className="text-center text-sm text-carrot-50/60">Cargando planes...</p>;
  if (error) return <p className="text-center text-sm text-red-400">{error}</p>;

  return (
    <div className="grid gap-6 lg:grid-cols-3">
      {plans?.map((plan) => (
        <PlanCard key={plan.id} plan={plan} />
      ))}
    </div>
  );
}
</file>

<file path="src/components/islands/POSIsland.tsx">
import { useEffect, useMemo, useState } from "react";
import { Minus, Plus, Trash2 } from "lucide-react";
import { getVariants, variantDisplayName } from "@/data/products";
import {
  fetchCategories,
  fetchProducts,
  fetchActiveOrders,
  submitPosSale,
  lookupLoyaltyCustomer,
  type ActiveOrder,
} from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";
import { useStaffSession } from "@/hooks/useStaffSession";
import StaffLogin from "@/components/islands/StaffLogin";
import { cn, formatMXN } from "@/lib/utils";
import { computeSubscriptionDiscount } from "@/lib/subscriptionDiscount";

type Tile = { id: string; name: string; price: number; emoji: string; categorySlug: string };

type TicketLine = { id: string; name: string; price: number; qty: number; emoji: string };
type Payment = "cash" | "card";

const STATUS_LABEL: Record<string, string> = { pending: "Recibido", preparing: "En preparación", ready: "Listo" };

export default function POSIsland() {
  const { employee, loading: sessionLoading, login, logout } = useStaffSession();
  const [category, setCategory] = useState<string>("Todos");
  const [ticket, setTicket] = useState<TicketLine[]>([]);
  const [payment, setPayment] = useState<Payment>("cash");
  const [cashGiven, setCashGiven] = useState("");
  const [customerContact, setCustomerContact] = useState("");
  const [subscriberPlanId, setSubscriberPlanId] = useState<string | null>(null);
  const [receipt, setReceipt] = useState<{ id: string; total: number } | null>(null);
  const [charging, setCharging] = useState(false);
  const [chargeError, setChargeError] = useState("");
  const [queue, setQueue] = useState<ActiveOrder[]>([]);
  const { data: categories, error: categoriesError, loading: categoriesLoading } = useSupabaseData(fetchCategories);
  const { data: products, error: productsError, loading: productsLoading } = useSupabaseData(fetchProducts);

  function refreshQueue() {
    if (!employee) return;
    fetchActiveOrders().then(setQueue).catch(() => {});
  }

  useEffect(refreshQueue, [employee]);

  const tiles = useMemo<Tile[]>(() => {
    if (!products) return [];
    const emojiBySlug: Record<string, string> = {};
    categories?.forEach((c) => (emojiBySlug[c.slug] = c.emoji));
    return products.flatMap((product) =>
      getVariants(product).map((variant) => ({
        id: variant.id,
        name: variantDisplayName(product, variant),
        price: variant.price,
        emoji: emojiBySlug[product.categorySlug] ?? "🍽️",
        categorySlug: product.categorySlug,
      }))
    );
  }, [products, categories]);

  const filtered = useMemo(
    () => tiles.filter((t) => category === "Todos" || t.categorySlug === category),
    [tiles, category]
  );

  const subtotal = ticket.reduce((sum, l) => sum + l.qty * l.price, 0);
  const subscriptionDiscount = useMemo(
    () => computeSubscriptionDiscount(subscriberPlanId, ticket, subtotal),
    [subscriberPlanId, ticket, subtotal]
  );
  const total = Math.max(subtotal - (subscriptionDiscount?.amount ?? 0), 0);
  const change = payment === "cash" ? Math.max(Number(cashGiven || 0) - total, 0) : 0;
  const loading = categoriesLoading || productsLoading;
  const error = categoriesError || productsError;

  async function lookupSubscriberPlan(contact: string) {
    const trimmed = contact.trim();
    if (!trimmed) {
      setSubscriberPlanId(null);
      return;
    }
    try {
      const profile = await lookupLoyaltyCustomer(trimmed);
      setSubscriberPlanId(profile?.activePlanId ?? null);
    } catch {
      setSubscriberPlanId(null);
    }
  }

  function addLine(tile: Tile) {
    setTicket((prev) => {
      const existing = prev.find((l) => l.id === tile.id);
      if (existing) {
        return prev.map((l) => (l.id === tile.id ? { ...l, qty: l.qty + 1 } : l));
      }
      return [...prev, { id: tile.id, name: tile.name, price: tile.price, qty: 1, emoji: tile.emoji }];
    });
  }

  function changeQty(id: string, delta: number) {
    setTicket((prev) =>
      prev
        .map((l) => (l.id === id ? { ...l, qty: l.qty + delta } : l))
        .filter((l) => l.qty > 0)
    );
  }

  async function charge() {
    if (ticket.length === 0) return;
    if (payment === "cash" && Number(cashGiven || 0) < total) return;
    setCharging(true);
    setChargeError("");
    try {
      const orderId = await submitPosSale({
        paymentMethod: payment,
        subtotal,
        discount: subscriptionDiscount?.amount ?? 0,
        total,
        items: ticket.map((l) => ({ name: l.name, price: l.price, qty: l.qty })),
      });
      setReceipt({ id: orderId.slice(0, 8), total });
      setTicket([]);
      setCashGiven("");
      setCustomerContact("");
      setSubscriberPlanId(null);
      refreshQueue();
    } catch (err) {
      setChargeError(err instanceof Error ? err.message : "No se pudo registrar la venta.");
    } finally {
      setCharging(false);
    }
  }

  if (sessionLoading) return <p className="text-sm text-carrot-50/60">Cargando sesión...</p>;
  if (!employee) return <StaffLogin onLogin={login} title="Acceso al punto de venta" />;

  return (
    <div className="space-y-8">
      <div className="flex items-center justify-between">
        <p className="text-sm text-carrot-50/70">
          Caja abierta por <span className="font-semibold text-carrot-50">{employee.name}</span>
        </p>
        <button
          type="button"
          onClick={logout}
          className="glass glass-card rounded-full px-4 py-2 text-xs font-semibold text-carrot-50/80"
        >
          Cerrar sesión
        </button>
      </div>

      <div className="grid gap-5 lg:grid-cols-[1.7fr_1fr]">
        <div className="glass rounded-2xl p-4 sm:p-5">
          <div className="flex flex-wrap gap-2">
            <button
              type="button"
              onClick={() => setCategory("Todos")}
              className={cn(
                "rounded-full px-3 py-2 text-xs font-semibold transition",
                category === "Todos" ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75"
              )}
            >
              Todos
            </button>
            {categories?.map((c) => (
              <button
                key={c.slug}
                type="button"
                onClick={() => setCategory(c.slug)}
                className={cn(
                  "rounded-full px-3 py-2 text-xs font-semibold transition",
                  category === c.slug ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75"
                )}
              >
                {c.emoji} {c.name}
              </button>
            ))}
          </div>

          {loading ? (
            <p className="mt-5 text-center text-sm text-carrot-50/60">Cargando productos...</p>
          ) : error ? (
            <p className="mt-5 text-center text-sm text-red-400">{error}</p>
          ) : (
            <div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3">
              {filtered.map((tile) => (
                <button
                  key={tile.id}
                  type="button"
                  onClick={() => addLine(tile)}
                  className="glass glass-card flex flex-col items-center rounded-2xl p-4 text-center active:scale-95"
                >
                  <span className="text-3xl">{tile.emoji}</span>
                  <span className="mt-2 text-xs font-semibold leading-tight">{tile.name}</span>
                  <span className="mt-1 text-xs text-carrot-300">{formatMXN(tile.price)}</span>
                </button>
              ))}
            </div>
          )}
        </div>

        <div className="glass-strong flex h-fit flex-col rounded-2xl p-5">
          <h2 className="text-sm font-semibold uppercase tracking-wider text-carrot-300">Ticket actual</h2>

          {receipt ? (
            <div className="mt-6 rounded-2xl bg-leaf-500/15 p-5 text-center">
              <span className="text-3xl">✅</span>
              <p className="mt-2 text-sm font-semibold">Venta #{receipt.id} cobrada</p>
              <p className="text-xs text-carrot-50/60">Total {formatMXN(receipt.total)}</p>
              <button
                type="button"
                onClick={() => setReceipt(null)}
                className="mt-4 w-full rounded-full bg-carrot-500 py-2 text-xs font-semibold text-ink-950"
              >
                Nueva venta
              </button>
            </div>
          ) : (
            <>
              {ticket.length === 0 ? (
                <p className="mt-6 text-center text-sm text-carrot-50/50">Toca un producto para agregarlo</p>
              ) : (
                <ul className="mt-4 max-h-72 space-y-2 overflow-y-auto pr-1">
                  {ticket.map((line) => (
                    <li key={line.id} className="flex items-center gap-2 rounded-xl bg-carrot-50/5 p-2.5">
                      <span className="text-lg">{line.emoji}</span>
                      <div className="flex-1">
                        <p className="text-xs font-medium">{line.name}</p>
                        <p className="text-[11px] text-carrot-50/50">{formatMXN(line.price)} c/u</p>
                      </div>
                      <button
                        type="button"
                        onClick={() => changeQty(line.id, -1)}
                        className="grid h-6 w-6 place-items-center rounded-full bg-carrot-50/10"
                      >
                        <Minus size={11} />
                      </button>
                      <span className="w-4 text-center text-xs font-semibold">{line.qty}</span>
                      <button
                        type="button"
                        onClick={() => changeQty(line.id, 1)}
                        className="grid h-6 w-6 place-items-center rounded-full bg-carrot-500 text-ink-950"
                      >
                        <Plus size={11} />
                      </button>
                      <button
                        type="button"
                        onClick={() => changeQty(line.id, -line.qty)}
                        className="text-carrot-50/30 hover:text-red-400"
                      >
                        <Trash2 size={13} />
                      </button>
                    </li>
                  ))}
                </ul>
              )}

              <div className="mt-4">
                <input
                  type="text"
                  value={customerContact}
                  onChange={(e) => setCustomerContact(e.target.value)}
                  onBlur={(e) => lookupSubscriberPlan(e.target.value)}
                  placeholder="Cliente: correo o teléfono (opcional)"
                  className="w-full rounded-xl bg-carrot-50/10 px-3 py-2 text-xs placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
                />
                {subscriptionDiscount && (
                  <p className="mt-1.5 text-xs text-leaf-300">🥕 {subscriptionDiscount.label} ✓</p>
                )}
              </div>

              {subscriptionDiscount && (
                <div className="mt-3 flex justify-between text-xs text-carrot-50/60">
                  <span>Subtotal</span>
                  <span>{formatMXN(subtotal)}</span>
                </div>
              )}
              <div className="mt-1 flex justify-between border-t border-carrot-50/15 pt-3 text-lg font-bold">
                <span>Total</span>
                <span>{formatMXN(total)}</span>
              </div>

              <div className="mt-4 grid grid-cols-2 gap-2">
                {(["cash", "card"] as Payment[]).map((opt) => (
                  <button
                    key={opt}
                    type="button"
                    onClick={() => setPayment(opt)}
                    className={cn(
                      "rounded-xl py-2 text-xs font-semibold transition",
                      payment === opt ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75"
                    )}
                  >
                    {opt === "cash" ? "💵 Efectivo" : "💳 Tarjeta"}
                  </button>
                ))}
              </div>

              {payment === "cash" && (
                <div className="mt-3">
                  <input
                    type="number"
                    value={cashGiven}
                    onChange={(e) => setCashGiven(e.target.value)}
                    placeholder="Efectivo recibido"
                    className="w-full rounded-xl bg-carrot-50/10 px-3 py-2 text-sm placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
                  />
                  {Number(cashGiven || 0) >= total && total > 0 && (
                    <p className="mt-1.5 text-xs text-leaf-300">Cambio: {formatMXN(change)}</p>
                  )}
                </div>
              )}

              {chargeError && <p className="mt-3 text-xs text-red-400">{chargeError}</p>}

              <button
                type="button"
                onClick={charge}
                disabled={charging || ticket.length === 0 || (payment === "cash" && Number(cashGiven || 0) < total)}
                className="mt-4 w-full rounded-full bg-leaf-500 py-3 text-sm font-semibold text-ink-950 transition hover:bg-leaf-400 disabled:cursor-not-allowed disabled:opacity-40"
              >
                {charging ? "Cobrando..." : "Cobrar"}
              </button>
            </>
          )}
        </div>
      </div>

      <div className="glass rounded-2xl p-5">
        <h2 className="text-sm font-semibold uppercase tracking-wider text-carrot-300">Comandas activas</h2>
        {queue.length === 0 ? (
          <p className="mt-4 text-sm text-carrot-50/50">No hay comandas pendientes.</p>
        ) : (
          <div className="mt-4 grid gap-3 sm:grid-cols-3">
            {queue.map((order) => (
              <div key={order.id} className="glass glass-card rounded-xl p-4">
                <div className="flex items-center justify-between">
                  <span className="font-mono text-xs text-carrot-50/50">#{order.id.slice(0, 8)}</span>
                  <span
                    className={cn(
                      "rounded-full px-2 py-0.5 text-[10px] font-semibold",
                      order.status === "ready" ? "bg-leaf-500/20 text-leaf-300" : "bg-carrot-500/20 text-carrot-300"
                    )}
                  >
                    {STATUS_LABEL[order.status] ?? order.status}
                  </span>
                </div>
                <p className="mt-2 text-sm text-carrot-50/80">
                  {order.items.map((i) => `${i.qty}x ${i.name}`).join(", ")}
                </p>
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  );
}
</file>

<file path="src/components/islands/PromotionsBoard.tsx">
import { useState } from "react";
import { getCheapest, getVariants } from "@/data/products";
import { fetchBestSellers, fetchCoupons, fetchNewestProducts, fetchProducts } from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";
import { formatMXN } from "@/lib/utils";

function CouponCard({ coupon }: { coupon: Awaited<ReturnType<typeof fetchCoupons>>[number] }) {
  const [copied, setCopied] = useState(false);

  function copy() {
    navigator.clipboard?.writeText(coupon.code).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 1800);
    });
  }

  return (
    <div className="glass glass-card flex flex-col rounded-2xl p-6">
      <div className="flex items-start justify-between gap-3">
        <div>
          <h3 className="font-semibold">{coupon.title}</h3>
          <p className="mt-1 text-sm text-carrot-50/65">{coupon.description}</p>
        </div>
        {coupon.onlyOnline && (
          <span className="shrink-0 rounded-full bg-leaf-500/20 px-2 py-1 text-[10px] font-semibold text-leaf-300">
            Solo web
          </span>
        )}
      </div>
      <div className="mt-5 flex items-center justify-between">
        <button
          type="button"
          onClick={copy}
          className="glass flex items-center gap-2 rounded-full px-4 py-2 font-mono text-sm font-semibold text-carrot-200"
        >
          {coupon.code}
          <span className="text-xs text-carrot-50/50">{copied ? "¡copiado!" : "copiar"}</span>
        </button>
        <span className="text-xs text-carrot-50/50">Vigente hasta {coupon.validUntil}</span>
      </div>
    </div>
  );
}

function ProductPromoCard({ name, subtitle, price }: { name: string; subtitle: string; price: number }) {
  return (
    <div className="glass glass-card rounded-2xl p-5">
      <h3 className="font-semibold">{name}</h3>
      <p className="mt-1 text-xs text-carrot-50/65">{subtitle}</p>
      <p className="mt-3 font-bold text-carrot-200">{formatMXN(price)}</p>
    </div>
  );
}

export default function PromotionsBoard() {
  const { data: coupons, error: couponsError, loading: couponsLoading } = useSupabaseData(fetchCoupons);
  const { data: products, error: productsError, loading: productsLoading } = useSupabaseData(fetchProducts);
  const { data: newest, error: newestError, loading: newestLoading } = useSupabaseData(() => fetchNewestProducts(6));
  const {
    data: bestSellers,
    error: bestSellersError,
    loading: bestSellersLoading,
  } = useSupabaseData(() => fetchBestSellers(6));

  const cheapest = products ? getCheapest(products, 6) : [];

  return (
    <>
      {couponsLoading ? (
        <p className="text-center text-sm text-carrot-50/60">Cargando cupones...</p>
      ) : couponsError ? (
        <p className="text-center text-sm text-red-400">{couponsError}</p>
      ) : (
        <section className="grid gap-5 sm:grid-cols-2">
          {coupons?.map((coupon) => (
            <CouponCard key={coupon.code} coupon={coupon} />
          ))}
        </section>
      )}

      {newestLoading ? (
        <p className="mt-16 text-center text-sm text-carrot-50/60">Cargando novedades...</p>
      ) : newestError ? (
        <p className="mt-16 text-center text-sm text-red-400">{newestError}</p>
      ) : newest && newest.length > 0 ? (
        <section className="mt-16">
          <h2 className="text-2xl font-bold">🆕 Novedades</h2>
          <div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
            {newest.map((p) => {
              const variant = getVariants(p)[0];
              return <ProductPromoCard key={p.id} name={p.name} subtitle={p.description} price={variant.price} />;
            })}
          </div>
        </section>
      ) : null}

      {bestSellersLoading ? (
        <p className="mt-16 text-center text-sm text-carrot-50/60">Cargando más vendidos...</p>
      ) : bestSellersError ? (
        <p className="mt-16 text-center text-sm text-red-400">{bestSellersError}</p>
      ) : bestSellers && bestSellers.length > 0 ? (
        <section className="mt-16">
          <h2 className="text-2xl font-bold">🔥 Los más vendidos</h2>
          <div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
            {bestSellers.map((item) => (
              <ProductPromoCard
                key={item.name}
                name={item.name}
                subtitle={`${item.unitsSold} ${item.unitsSold === 1 ? "vendido" : "vendidos"}`}
                price={item.price}
              />
            ))}
          </div>
        </section>
      ) : null}

      {productsLoading ? (
        <p className="mt-16 text-center text-sm text-carrot-50/60">Cargando destacados...</p>
      ) : productsError ? (
        <p className="mt-16 text-center text-sm text-red-400">{productsError}</p>
      ) : (
        <section className="mt-16">
          <h2 className="text-2xl font-bold">🪙 Los más económicos</h2>
          <div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
            {cheapest.map((p) => {
              const variant = getVariants(p)[0];
              return <ProductPromoCard key={p.id} name={p.name} subtitle={p.description} price={variant.price} />;
            })}
          </div>
        </section>
      )}
    </>
  );
}
</file>

<file path="src/components/islands/QrPreview.tsx">
import { useEffect, useState } from "react";
import QRCode from "qrcode";

/** Real, scannable QR code encoding the customer's member code. */
export default function QrPreview({ seed, size = 220 }: { seed: string; size?: number }) {
  const [dataUrl, setDataUrl] = useState<string | null>(null);

  useEffect(() => {
    let active = true;
    QRCode.toDataURL(`DONZANAHORIO:${seed}`, {
      width: size,
      margin: 1,
      color: { dark: "#1c1305", light: "#fdf6e3" },
    }).then((url) => {
      if (active) setDataUrl(url);
    });
    return () => {
      active = false;
    };
  }, [seed, size]);

  return (
    <div className="grid aspect-square w-full max-w-[10rem] place-items-center rounded-xl bg-carrot-50 p-3">
      {dataUrl ? (
        <img src={dataUrl} alt={`Código QR de cliente ${seed}`} className="h-full w-full" />
      ) : (
        <span className="text-xs text-ink-950/50">Generando QR...</span>
      )}
    </div>
  );
}
</file>

<file path="src/components/islands/StarRating.tsx">
import { Star } from "lucide-react";
import { cn } from "@/lib/utils";

export default function StarRating({ value, onChange }: { value: number; onChange: (v: number) => void }) {
  return (
    <div className="flex gap-1">
      {[1, 2, 3, 4, 5].map((n) => (
        <button
          key={n}
          type="button"
          aria-label={`${n} estrellas`}
          onClick={() => onChange(n)}
          className="p-0.5"
        >
          <Star
            size={26}
            className={cn(n <= value ? "fill-carrot-400 text-carrot-400" : "text-carrot-50/25")}
          />
        </button>
      ))}
    </div>
  );
}
</file>

<file path="src/components/islands/SurveyForm.tsx">
import { useState } from "react";
import StarRating from "@/components/islands/StarRating";
import GoogleReviewLink from "@/components/islands/GoogleReviewLink";
import { SITE } from "@/data/site";
import { fetchCashiers, submitSurvey } from "@/lib/queries";
import { useSupabaseData } from "@/hooks/useSupabaseData";
import { cn } from "@/lib/utils";

export default function SurveyForm() {
  const { data: cashiers, error: cashiersError, loading: cashiersLoading } = useSupabaseData(fetchCashiers);
  const [branchRating, setBranchRating] = useState(0);
  const [serviceRating, setServiceRating] = useState(0);
  const [cashierId, setCashierId] = useState<string | null>(null);
  const [comments, setComments] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const [submitting, setSubmitting] = useState(false);
  const [submitError, setSubmitError] = useState("");

  const canSubmit = branchRating > 0 && serviceRating > 0;

  if (submitted) {
    return (
      <div className="glass-strong mx-auto max-w-lg rounded-3xl p-8 text-center">
        <span className="text-5xl">🙌</span>
        <h2 className="mt-4 text-2xl font-bold">¡Gracias por tu opinión!</h2>
        <p className="mt-2 text-sm text-carrot-50/70">
          Tu encuesta ayuda a mejorar el servicio y a reconocer a nuestro equipo. Como agradecimiento, comparte tu
          experiencia también en Google y recibe un cupón especial.
        </p>
        <GoogleReviewLink className="mt-6" />
      </div>
    );
  }

  return (
    <form
      className="glass mx-auto max-w-xl space-y-7 rounded-2xl p-6 sm:p-8"
      onSubmit={async (e) => {
        e.preventDefault();
        if (!canSubmit) return;
        setSubmitting(true);
        setSubmitError("");
        try {
          await submitSurvey({
            branchRating,
            serviceRating,
            cashierId,
            comments: comments.trim() || null,
          });
          setSubmitted(true);
        } catch (err) {
          setSubmitError(err instanceof Error ? err.message : "No se pudo enviar tu encuesta. Intenta de nuevo.");
        } finally {
          setSubmitting(false);
        }
      }}
    >
      <div>
        <p className="mb-2 text-sm font-semibold">¿Cómo calificas la sucursal ({SITE.city})?</p>
        <StarRating value={branchRating} onChange={setBranchRating} />
      </div>

      <div>
        <p className="mb-2 text-sm font-semibold">¿Cómo calificas el servicio recibido?</p>
        <StarRating value={serviceRating} onChange={setServiceRating} />
      </div>

      <div>
        <p className="mb-2 text-sm font-semibold">Vota por tu cajero(a) favorito(a)</p>
        {cashiersLoading ? (
          <p className="text-xs text-carrot-50/50">Cargando cajeros...</p>
        ) : cashiersError ? (
          <p className="text-xs text-red-400">{cashiersError}</p>
        ) : (
          <div className="grid grid-cols-2 gap-2 sm:grid-cols-4">
            {cashiers?.map((c) => (
              <button
                key={c.id}
                type="button"
                onClick={() => setCashierId(c.id)}
                className={cn(
                  "rounded-xl p-3 text-center text-xs transition",
                  cashierId === c.id ? "bg-carrot-500 text-ink-950" : "bg-carrot-50/10 text-carrot-50/75 hover:bg-carrot-50/20"
                )}
              >
                <span className="block text-xl">{c.emoji}</span>
                <span className="mt-1 block font-semibold">{c.name}</span>
              </button>
            ))}
          </div>
        )}
        <p className="mt-2 text-xs text-carrot-50/50">
          Los votos alimentan el sistema de incentivos y bonos del personal.
        </p>
      </div>

      <div>
        <label className="mb-2 block text-sm font-semibold">Comentarios (opcional)</label>
        <textarea
          value={comments}
          onChange={(e) => setComments(e.target.value)}
          rows={3}
          placeholder="Cuéntanos más sobre tu visita..."
          className="w-full rounded-xl bg-carrot-50/10 px-4 py-2.5 text-sm placeholder:text-carrot-50/40 focus:outline-none focus:ring-2 focus:ring-carrot-400"
        />
      </div>

      {submitError && <p className="text-sm text-red-400">{submitError}</p>}

      <button
        type="submit"
        disabled={!canSubmit || submitting}
        className="w-full rounded-full bg-carrot-500 py-3 text-sm font-semibold text-ink-950 transition hover:bg-carrot-400 disabled:cursor-not-allowed disabled:opacity-40"
      >
        {submitting ? "Enviando..." : "Enviar encuesta"}
      </button>
    </form>
  );
}
</file>

<file path="src/components/Footer.astro">
---
import { NAV_LINKS, PORTAL_LINKS, SITE, whatsappLink } from "@/data/site";
---

<footer class="mt-24 px-3 pb-6 sm:px-6">
  <div class="glass mx-auto max-w-6xl rounded-3xl p-6 sm:p-10">
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
      <div>
        <div class="flex items-center gap-2">
          <span class="grid h-9 w-9 place-items-center rounded-full bg-gradient-to-br from-carrot-400 to-leaf-500 text-lg">
            🥕
          </span>
          <span class="font-semibold text-carrot-50">{SITE.name}</span>
        </div>
        <p class="mt-3 text-sm text-carrot-50/70">{SITE.tagline}</p>
        <div class="mt-4 flex gap-2">
          <a
            href={SITE.social.instagram}
            target="_blank"
            rel="noopener noreferrer"
            class="glass glass-card rounded-full px-3 py-1.5 text-xs text-carrot-50/80"
            >Instagram</a
          >
          <a
            href={SITE.social.facebook}
            target="_blank"
            rel="noopener noreferrer"
            class="glass glass-card rounded-full px-3 py-1.5 text-xs text-carrot-50/80"
            >Facebook</a
          >
        </div>
      </div>

      <div>
        <p class="text-xs font-semibold uppercase tracking-wider text-carrot-300">Explora</p>
        <ul class="mt-3 space-y-2 text-sm text-carrot-50/75">
          {NAV_LINKS.map((link) => (
            <li><a href={link.href} class="hover:text-carrot-200">{link.label}</a></li>
          ))}
        </ul>
      </div>

      <div>
        <p class="text-xs font-semibold uppercase tracking-wider text-carrot-300">Portal interno</p>
        <ul class="mt-3 space-y-2 text-sm text-carrot-50/75">
          {PORTAL_LINKS.map((link) => (
            <li><a href={link.href} class="hover:text-carrot-200">{link.label}</a></li>
          ))}
        </ul>
      </div>

      <div>
        <p class="text-xs font-semibold uppercase tracking-wider text-carrot-300">Visítanos</p>
        <ul class="mt-3 space-y-2 text-sm text-carrot-50/75">
          <li>{SITE.address}</li>
          <li>{SITE.city}, {SITE.state}, {SITE.country}</li>
          <li>
            <a href={`tel:+${SITE.phoneE164}`} class="hover:text-carrot-200">{SITE.phoneDisplay}</a>
          </li>
          <li>
            <a href={whatsappLink("¡Hola! Tengo una pregunta")} class="hover:text-carrot-200">WhatsApp</a>
          </li>
        </ul>
      </div>
    </div>

    <div class="mt-8 flex flex-col gap-2 border-t border-carrot-50/10 pt-6 text-xs text-carrot-50/50 sm:flex-row sm:items-center sm:justify-between">
      <p>© {new Date().getFullYear()} {SITE.name}. Demo funcional con datos de ejemplo.</p>
      <p>Hecho con 🥕 en {SITE.city}, {SITE.state}</p>
    </div>
  </div>
</footer>
</file>

<file path="src/components/Navbar.astro">
---
import { NAV_LINKS, PORTAL_LINKS, SITE, whatsappLink } from "@/data/site";
import CartBadge from "@/components/islands/CartBadge";

const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-40 px-3 pt-3 sm:px-6 sm:pt-4">
  <nav class="glass-strong mx-auto flex max-w-6xl items-center justify-between rounded-2xl px-4 py-2.5 sm:px-5">
    <a href="/" class="flex items-center gap-2 shrink-0">
      <span class="grid h-9 w-9 place-items-center rounded-full bg-gradient-to-br from-carrot-400 to-leaf-500 text-lg shadow-glass">
        🥕
      </span>
      <span class="text-sm font-semibold tracking-wide text-carrot-50">
        {SITE.name}
      </span>
    </a>

    <div class="hidden items-center gap-1 lg:flex">
      {NAV_LINKS.map((link) => (
        <a
          href={link.href}
          class:list={[
            "rounded-full px-3 py-1.5 text-sm transition-colors",
            currentPath === link.href
              ? "bg-carrot-500/20 text-carrot-200"
              : "text-carrot-50/80 hover:text-carrot-200",
          ]}
        >
          {link.label}
        </a>
      ))}
      <div class="mx-1 h-5 w-px bg-carrot-50/15"></div>
      <details class="group relative">
        <summary class="flex cursor-pointer list-none items-center gap-1 rounded-full px-3 py-1.5 text-sm text-carrot-50/80 hover:text-carrot-200">
          Portal interno
        </summary>
        <div class="glass-strong bg-ink-950 absolute right-0 mt-2 w-56 rounded-xl p-1.5">
          {PORTAL_LINKS.map((link) => (
            <a
              href={link.href}
              class="block rounded-lg px-3 py-2 text-sm text-carrot-50/85 hover:bg-carrot-50/10 hover:text-carrot-200"
            >
              {link.label}
            </a>
          ))}
        </div>
      </details>
    </div>

    <div class="flex items-center gap-2">
      <a
        href={whatsappLink("¡Hola! Quiero hacer un pedido en Don Zanahorio 🥕")}
        target="_blank"
        rel="noopener noreferrer"
        class="hidden rounded-full bg-leaf-500 px-4 py-2 text-sm font-semibold text-ink-950 shadow-glass transition hover:bg-leaf-400 sm:block"
      >
        Pedir por WhatsApp
      </a>
      <CartBadge client:load />
      <details class="group relative lg:hidden">
        <summary class="glass flex h-11 w-11 cursor-pointer list-none items-center justify-center rounded-full text-carrot-100">
          <span class="sr-only">Menú</span>
          ☰
        </summary>
        <div class="glass-strong bg-ink-950 absolute right-0 mt-2 w-60 rounded-xl p-1.5">
          {NAV_LINKS.map((link) => (
            <a href={link.href} class="block rounded-lg px-3 py-2 text-sm text-carrot-50/85 hover:bg-carrot-50/10">
              {link.label}
            </a>
          ))}
          <div class="my-1 h-px bg-carrot-50/15"></div>
          {PORTAL_LINKS.map((link) => (
            <a href={link.href} class="block rounded-lg px-3 py-2 text-sm text-carrot-50/85 hover:bg-carrot-50/10">
              {link.label}
            </a>
          ))}
        </div>
      </details>
    </div>
  </nav>
</header>
</file>

<file path="src/data/inventory.ts">
export type InventoryItem = {
  ingredient: string;
  stock: number;
  unit: string;
  min: number;
};

export const INVENTORY: InventoryItem[] = [
  { ingredient: "Zanahoria fresca", stock: 38, unit: "kg", min: 15 },
  { ingredient: "Pan artesanal", stock: 6, unit: "paquetes", min: 10 },
  { ingredient: "Queso crema", stock: 4, unit: "kg", min: 5 },
  { ingredient: "Aguacate", stock: 22, unit: "kg", min: 8 },
  { ingredient: "Vasos para jugo", stock: 3, unit: "paquetes", min: 6 },
  { ingredient: "Pechuga de pollo", stock: 18, unit: "kg", min: 10 },
];

export function isLowStock(item: InventoryItem) {
  return item.stock <= item.min;
}
</file>

<file path="src/data/plans.ts">
export type Plan = {
  id: string;
  name: string;
  price: number;
  period: "mes";
  emoji: string;
  perk: string;
  benefits: string[];
  highlight?: boolean;
};
</file>

<file path="src/data/products.ts">
export type Category = {
  id: string;
  slug: string;
  name: string;
  emoji: string;
};

export type Product = {
  id: string;
  name: string;
  description: string;
  categorySlug: string;
  categoryName: string;
  priceMedium: number | null;
  priceLarge: number | null;
};

export type ProductVariant = {
  id: string;
  sizeLabel: "Mediano" | "Grande" | null;
  price: number;
};

/** Un producto con un solo precio se vende en talla única; con ambos, el cliente elige. */
export function getVariants(product: Product): ProductVariant[] {
  const hasBoth = product.priceMedium != null && product.priceLarge != null;
  const variants: ProductVariant[] = [];
  if (product.priceMedium != null) {
    variants.push({ id: `${product.id}--mediano`, sizeLabel: hasBoth ? "Mediano" : null, price: product.priceMedium });
  }
  if (product.priceLarge != null) {
    variants.push({ id: `${product.id}--grande`, sizeLabel: hasBoth ? "Grande" : null, price: product.priceLarge });
  }
  return variants;
}

export function variantDisplayName(product: Product, variant: ProductVariant) {
  return variant.sizeLabel ? `${product.name} (${variant.sizeLabel})` : product.name;
}

export function getCheapest(products: Product[], count: number): Product[] {
  return [...products]
    .sort((a, b) => (a.priceMedium ?? a.priceLarge ?? 0) - (b.priceMedium ?? b.priceLarge ?? 0))
    .slice(0, count);
}
</file>

<file path="src/data/promotions.ts">
export type Coupon = {
  code: string;
  title: string;
  description: string;
  discount: string;
  discountPercent?: number;
  validUntil: string;
  onlyOnline?: boolean;
};
</file>

<file path="src/data/site.ts">
export const SITE = {
  name: "Don Zanahorio",
  tagline: "Desayunos, comida corrida y antojitos caseros para toda la familia",
  city: "Ciudad Victoria",
  state: "Tamaulipas",
  country: "México",
  address: "Blvd. Praxedis Balboa 1420, Col. Las Palmas, Cd. Victoria, Tamps.",
  phoneDisplay: "834 123 4567",
  phoneE164: "5218341234567",
  email: "hola@donzanahorio.mx",
  hours: [
    { day: "Lunes a viernes", time: "8:00 am – 9:00 pm" },
    { day: "Sábado", time: "9:00 am – 10:00 pm" },
    { day: "Domingo", time: "9:00 am – 6:00 pm" },
  ],
  social: {
    instagram: "https://instagram.com/donzanahorio",
    facebook: "https://facebook.com/donzanahorio",
  },
  googleReviewUrl: "https://g.page/r/donzanahorio/review",
  shipping: {
    freeThreshold: 500,
    baseCost: 45,
    perKm: 6,
  },
} as const;

export function whatsappLink(message: string) {
  const encoded = encodeURIComponent(message);
  return `https://wa.me/${SITE.phoneE164}?text=${encoded}`;
}

export const NAV_LINKS = [
  { href: "/menu", label: "Menú" },
  { href: "/mayoreo", label: "Mayoreo" },
  { href: "/fidelizacion", label: "Fidelización" },
  { href: "/suscripciones", label: "Suscripciones" },
  { href: "/promociones", label: "Promociones" },
  { href: "/encuestas", label: "Encuestas" },
] as const;

export const PORTAL_LINKS = [
  { href: "/pos", label: "Punto de venta (POS)" },
  { href: "/empleados", label: "Portal de empleados" },
  { href: "/admin", label: "Panel administrativo" },
] as const;
</file>

<file path="src/data/team.ts">
export type Testimonial = {
  name: string;
  quote: string;
  rating: number;
};

export const TESTIMONIALS: Testimonial[] = [
  {
    name: "Karla M.",
    quote: "El bowl de quinoa es mi lugar seguro para comer rico y ligero en Victoria.",
    rating: 5,
  },
  {
    name: "Jorge R.",
    quote: "Pedí para la oficina y llegó todo caliente y a tiempo. El pastel de zanahoria es otro nivel.",
    rating: 5,
  },
  {
    name: "Daniela T.",
    quote: "El programa de puntos me regresó dinero real en mi monedero, súper fácil de usar.",
    rating: 4,
  },
];

export type Cashier = {
  id: string;
  name: string;
  role: string;
  emoji: string;
};

export type JobPosition = {
  id: string;
  title: string;
  summary: string;
  responsibilities: string[];
};
</file>

<file path="src/data/wholesale.ts">
export type WholesaleItem = {
  id: string;
  name: string;
  unit: string;
  pricePerUnit: number;
  minUnits: number;
};

export const WHOLESALE_ITEMS: WholesaleItem[] = [
  { id: "jugos-caja", name: "Jugos prensados en frío (caja x12)", unit: "caja", pricePerUnit: 480, minUnits: 5 },
  { id: "charolas-bocadillos", name: "Charola de bocadillos saludables", unit: "charola", pricePerUnit: 650, minUnits: 3 },
  { id: "pasteles-zanahoria", name: "Pastel de zanahoria entero", unit: "pastel", pricePerUnit: 420, minUnits: 2 },
  { id: "catering-persona", name: "Catering para evento (por persona)", unit: "persona", pricePerUnit: 145, minUnits: 20 },
  { id: "bowls-oficina", name: "Bowls saludables para oficina", unit: "bowl", pricePerUnit: 119, minUnits: 15 },
];

export const VOLUME_DISCOUNTS = [
  { minUnits: 100, discount: 0.15 },
  { minUnits: 50, discount: 0.1 },
  { minUnits: 20, discount: 0.05 },
];

export function discountFor(units: number) {
  const tier = VOLUME_DISCOUNTS.find((t) => units >= t.minUnits);
  return tier?.discount ?? 0;
}
</file>

<file path="src/hooks/useSupabaseData.ts">
import { useEffect, useState } from "react";

export function useSupabaseData<T>(fetcher: () => Promise<T>) {
  const [data, setData] = useState<T | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    let active = true;
    fetcher()
      .then((result) => {
        if (active) setData(result);
      })
      .catch((err) => {
        if (active) setError(err instanceof Error ? err.message : "No se pudo cargar la información.");
      });
    return () => {
      active = false;
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  return { data, error, loading: data === null && error === null };
}
</file>

<file path="src/layouts/Layout.astro">
---
import Navbar from "@/components/Navbar.astro";
import Footer from "@/components/Footer.astro";
import "@/styles/global.css";

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "Don Zanahorio — cocina fresca y saludable en Ciudad Victoria, Tamaulipas.",
} = Astro.props;
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title} · Don Zanahorio</title>
  </head>
  <body>
    <div class="ambient-bg">
      <div class="ambient-blob h-[26rem] w-[26rem] bg-carrot-500 -left-20 -top-20"></div>
      <div class="ambient-blob h-[22rem] w-[22rem] bg-leaf-500 -right-10 top-1/3" style="animation-delay: -6s"></div>
      <div class="ambient-blob h-[24rem] w-[24rem] bg-carrot-700 left-1/3 -bottom-24" style="animation-delay: -12s"></div>
    </div>

    <Navbar />
    <main class="mx-auto min-h-[60vh] max-w-6xl px-3 py-10 sm:px-6 sm:py-16">
      <slot />
    </main>
    <Footer />
  </body>
</html>
</file>

<file path="src/lib/queries.ts">
import { supabase } from "@/lib/supabase";
import type { Category, Product } from "@/data/products";
import type { Coupon } from "@/data/promotions";
import type { Plan } from "@/data/plans";
import type { Cashier, JobPosition } from "@/data/team";

export async function fetchCategories(): Promise<Category[]> {
  const { data, error } = await supabase
    .from("categories")
    .select("id, slug, name, emoji")
    .eq("active", true)
    .order("sort_order");
  if (error) throw error;

  return data.map((row) => ({
    id: row.id,
    slug: row.slug,
    name: row.name,
    emoji: row.emoji ?? "🍽️",
  }));
}

export async function fetchProducts(): Promise<Product[]> {
  const { data, error } = await supabase
    .from("products")
    .select("slug, name, description, price_medium, price_large, categories(slug, name)")
    .eq("active", true)
    .order("name");
  if (error) throw error;

  return data.map((row) => {
    const category = row.categories as unknown as { slug: string; name: string } | null;
    return {
      id: row.slug,
      name: row.name,
      description: row.description ?? "",
      categorySlug: category?.slug ?? "",
      categoryName: category?.name ?? "",
      priceMedium: row.price_medium !== null ? Number(row.price_medium) : null,
      priceLarge: row.price_large !== null ? Number(row.price_large) : null,
    };
  });
}

export async function fetchNewestProducts(limit: number): Promise<Product[]> {
  const { data, error } = await supabase
    .from("products")
    .select("slug, name, description, price_medium, price_large, categories(slug, name)")
    .eq("active", true)
    .order("created_at", { ascending: false })
    .limit(limit);
  if (error) throw error;

  return data.map((row) => {
    const category = row.categories as unknown as { slug: string; name: string } | null;
    return {
      id: row.slug,
      name: row.name,
      description: row.description ?? "",
      categorySlug: category?.slug ?? "",
      categoryName: category?.name ?? "",
      priceMedium: row.price_medium !== null ? Number(row.price_medium) : null,
      priceLarge: row.price_large !== null ? Number(row.price_large) : null,
    };
  });
}

export type BestSeller = { name: string; price: number; unitsSold: number };

// order_items is RLS-restricted to staff, so this reads through a
// SECURITY DEFINER RPC that returns only the aggregate (name/price/qty
// sold), never raw order or customer data, to anonymous visitors.
export async function fetchBestSellers(limit: number): Promise<BestSeller[]> {
  const { data, error } = await supabase.rpc("get_best_sellers", { p_limit: limit });
  if (error) throw error;
  return (data ?? []).map((row: { name: string; price: number; units_sold: number }) => ({
    name: row.name,
    price: Number(row.price),
    unitsSold: Number(row.units_sold),
  }));
}

export async function fetchCoupons(): Promise<Coupon[]> {
  const { data, error } = await supabase
    .from("coupons")
    .select("code, title, description, discount_label, discount_percent, valid_until, only_online")
    .eq("active", true)
    .order("created_at");
  if (error) throw error;

  return data.map((row) => ({
    code: row.code,
    title: row.title,
    description: row.description ?? "",
    discount: row.discount_label,
    discountPercent: row.discount_percent ?? undefined,
    validUntil: row.valid_until
      ? new Date(row.valid_until).toLocaleDateString("es-MX", {
          day: "numeric",
          month: "long",
          year: "numeric",
        })
      : "Vigencia permanente",
    onlyOnline: row.only_online,
  }));
}

export async function fetchPlans(): Promise<Plan[]> {
  const { data, error } = await supabase
    .from("subscription_plans")
    .select("slug, name, price, period, emoji, perk, benefits, highlight")
    .eq("active", true)
    .order("price");
  if (error) throw error;

  return data.map((row) => ({
    id: row.slug,
    name: row.name,
    price: Number(row.price),
    period: row.period as Plan["period"],
    emoji: row.emoji,
    perk: row.perk ?? "",
    benefits: row.benefits,
    highlight: row.highlight,
  }));
}

export async function fetchCashiers(): Promise<Cashier[]> {
  const { data, error } = await supabase
    .from("cashiers")
    .select("id, name, role, avatar_emoji")
    .eq("active", true)
    .order("name");
  if (error) throw error;

  return data.map((row) => ({
    id: row.id,
    name: row.name,
    role: row.role ?? "",
    emoji: row.avatar_emoji,
  }));
}

export async function fetchJobPositions(): Promise<JobPosition[]> {
  const { data, error } = await supabase
    .from("job_positions")
    .select("id, title, summary, responsibilities")
    .order("title");
  if (error) throw error;

  return data.map((row) => ({
    id: row.id,
    title: row.title,
    summary: row.summary ?? "",
    responsibilities: row.responsibilities,
  }));
}

// =========================================================
// Escrituras (formularios públicos)
// =========================================================

export async function submitOrder(params: {
  fulfillmentType: "pickup" | "delivery";
  address: string | null;
  paymentMethod: "card" | "cash";
  subtotal: number;
  shippingCost: number;
  discount: number;
  couponCode: string | null;
  total: number;
  items: Array<{ name: string; price: number; qty: number }>;
}) {
  const orderId = crypto.randomUUID();

  const { error: orderError } = await supabase.from("orders").insert({
    id: orderId,
    fulfillment_type: params.fulfillmentType,
    address: params.address,
    payment_method: params.paymentMethod,
    subtotal: params.subtotal,
    shipping_cost: params.shippingCost,
    discount: params.discount,
    coupon_code: params.couponCode,
    total: params.total,
  });
  if (orderError) throw orderError;

  const { error: itemsError } = await supabase.from("order_items").insert(
    params.items.map((item) => ({
      order_id: orderId,
      name: item.name,
      price: item.price,
      qty: item.qty,
    }))
  );
  if (itemsError) throw itemsError;

  return orderId;
}

export async function submitWholesaleOrder(params: {
  customerName: string;
  company: string | null;
  phone: string;
  itemName: string;
  units: number;
  unitPrice: number;
  fulfillmentType: "pickup" | "delivery";
  eventDate: string | null;
  notes: string | null;
  quoteTotal: number;
}) {
  const { error } = await supabase.from("wholesale_orders").insert({
    customer_name: params.customerName,
    company: params.company,
    phone: params.phone,
    item_name: params.itemName,
    units: params.units,
    unit_price: params.unitPrice,
    fulfillment_type: params.fulfillmentType,
    event_date: params.eventDate,
    notes: params.notes,
    quote_total: params.quoteTotal,
  });
  if (error) throw error;
}

export async function submitSurvey(params: {
  branchRating: number;
  serviceRating: number;
  cashierId: string | null;
  comments: string | null;
}) {
  const { error } = await supabase.from("surveys").insert({
    branch_rating: params.branchRating,
    service_rating: params.serviceRating,
    cashier_id: params.cashierId,
    comments: params.comments,
  });
  if (error) throw error;
}

export async function submitGoogleReviewClick() {
  await supabase.from("google_reviews").insert({});
}

export async function submitJobApplication(params: {
  name: string;
  phone: string;
  positionId: string;
  cvPath: string | null;
}) {
  const { error } = await supabase.from("job_applications").insert({
    name: params.name,
    phone: params.phone,
    position_id: params.positionId,
    cv_url: params.cvPath,
  });
  if (error) throw error;
}

export async function submitEmployeeReferral(params: {
  candidateName: string;
  candidatePhone: string;
  positionId: string;
}) {
  const { error } = await supabase.from("employee_referrals").insert({
    candidate_name: params.candidateName,
    candidate_phone: params.candidatePhone,
    position_id: params.positionId,
  });
  if (error) throw error;
}

// =========================================================
// Fidelización (respaldada por Supabase, no localStorage)
// =========================================================

export type LoyaltySnapshot = {
  id: string;
  name: string;
  email: string | null;
  phone: string | null;
  memberCode: string;
  points: number;
  walletBalance: number;
  giftCardBalance: number;
  referralCode: string;
  joinedAt: string;
  purchaseHistory: Array<{ date: string; orderId: string; total: number; pointsEarned: number }>;
  activePlanId: string | null;
};

function mapLoyaltySnapshot(row: Record<string, unknown>): LoyaltySnapshot {
  const history = (row.purchaseHistory as Array<Record<string, unknown>>) ?? [];
  return {
    id: row.id as string,
    name: row.name as string,
    email: (row.email as string) ?? null,
    phone: (row.phone as string) ?? null,
    memberCode: row.memberCode as string,
    points: Number(row.points),
    walletBalance: Number(row.walletBalance),
    giftCardBalance: Number(row.giftCardBalance),
    referralCode: row.referralCode as string,
    joinedAt: row.joinedAt as string,
    purchaseHistory: history.map((h) => ({
      date: h.date as string,
      orderId: h.orderId as string,
      total: Number(h.total),
      pointsEarned: Number(h.pointsEarned),
    })),
    activePlanId: (row.activePlanId as string) ?? null,
  };
}

export async function registerLoyaltyCustomer(
  name: string,
  contact: string,
  referralCode?: string | null
): Promise<LoyaltySnapshot> {
  const { data, error } = await supabase.rpc("register_loyalty_customer", {
    p_name: name,
    p_contact: contact,
    p_referral_code: referralCode || null,
  });
  if (error) {
    if (error.message.includes("ya_existe_cuenta")) {
      throw new Error("Ya existe una cuenta con ese correo o teléfono. Usa la pestaña 'Ya tengo cuenta'.");
    }
    throw error;
  }
  return mapLoyaltySnapshot(data);
}

export async function lookupLoyaltyCustomer(contact: string): Promise<LoyaltySnapshot | null> {
  const { data, error } = await supabase.rpc("get_loyalty_snapshot", { p_contact: contact });
  if (error) throw error;
  return data ? mapLoyaltySnapshot(data) : null;
}

export async function awardOrderPoints(orderId: string, contact: string): Promise<LoyaltySnapshot | null> {
  const { data, error } = await supabase.rpc("award_order_points", { p_order_id: orderId, p_contact: contact });
  if (error) throw error;
  return data ? mapLoyaltySnapshot(data) : null;
}

export async function markOrderPaid(orderId: string, stripeSessionId: string, internalSecret: string) {
  const { error } = await supabase.rpc("mark_order_paid", {
    p_order_id: orderId,
    p_stripe_session_id: stripeSessionId,
    p_internal_secret: internalSecret,
  });
  if (error) throw error;
}

// =========================================================
// Capacitación de empleados (respaldada por Supabase)
// =========================================================

export type TrainingModule = {
  id: string;
  title: string;
  type: string;
  duration: string;
  contentUrl: string | null;
};

export async function fetchTrainingModules(): Promise<TrainingModule[]> {
  const { data, error } = await supabase
    .from("training_modules")
    .select("id, title, type, duration, content_url")
    .order("title");
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    title: row.title,
    type: row.type,
    duration: row.duration ?? "",
    contentUrl: row.content_url,
  }));
}

export async function fetchTrainingProgress(employeeId: string): Promise<string[]> {
  const { data, error } = await supabase
    .from("employee_training_progress")
    .select("module_id")
    .eq("employee_id", employeeId);
  if (error) throw error;
  return data.map((row) => row.module_id);
}

export async function markTrainingComplete(employeeId: string, moduleId: string) {
  const { error } = await supabase
    .from("employee_training_progress")
    .insert({ employee_id: employeeId, module_id: moduleId });
  if (error) throw error;
}

// =========================================================
// Panel administrativo (requiere employees.role = 'admin', forzado por RLS)
// =========================================================

export type AdminOrder = {
  id: string;
  fulfillmentType: string;
  address: string | null;
  paymentMethod: string;
  subtotal: number;
  shippingCost: number;
  discount: number;
  couponCode: string | null;
  total: number;
  status: string;
  paymentStatus: string;
  createdAt: string;
  items: Array<{ name: string; price: number; qty: number }>;
};

export async function fetchAdminOrders(): Promise<AdminOrder[]> {
  const { data, error } = await supabase
    .from("orders")
    .select(
      "id, fulfillment_type, address, payment_method, subtotal, shipping_cost, discount, coupon_code, total, status, payment_status, created_at, order_items(name, price, qty)"
    )
    .order("created_at", { ascending: false });
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    fulfillmentType: row.fulfillment_type,
    address: row.address,
    paymentMethod: row.payment_method,
    subtotal: Number(row.subtotal),
    shippingCost: Number(row.shipping_cost),
    discount: Number(row.discount),
    couponCode: row.coupon_code,
    total: Number(row.total),
    status: row.status,
    paymentStatus: row.payment_status,
    createdAt: row.created_at,
    items: (row.order_items as Array<{ name: string; price: number; qty: number }>) ?? [],
  }));
}

export async function updateOrderStatus(orderId: string, status: string) {
  const { error } = await supabase.from("orders").update({ status }).eq("id", orderId);
  if (error) throw error;
}

export type AdminSurvey = {
  id: string;
  branchRating: number;
  serviceRating: number;
  cashierName: string | null;
  comments: string | null;
  createdAt: string;
};

export async function fetchAdminSurveys(): Promise<AdminSurvey[]> {
  const { data, error } = await supabase
    .from("surveys")
    .select("id, branch_rating, service_rating, comments, created_at, cashiers(name)")
    .order("created_at", { ascending: false });
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    branchRating: row.branch_rating,
    serviceRating: row.service_rating,
    cashierName: (row.cashiers as unknown as { name: string } | null)?.name ?? null,
    comments: row.comments,
    createdAt: row.created_at,
  }));
}

export type AdminWholesaleOrder = {
  id: string;
  customerName: string;
  company: string | null;
  phone: string;
  itemName: string;
  units: number;
  unitPrice: number | null;
  fulfillmentType: string;
  eventDate: string | null;
  notes: string | null;
  quoteTotal: number | null;
  status: string;
  createdAt: string;
};

export async function fetchAdminWholesaleOrders(): Promise<AdminWholesaleOrder[]> {
  const { data, error } = await supabase
    .from("wholesale_orders")
    .select(
      "id, customer_name, company, phone, item_name, units, unit_price, fulfillment_type, event_date, notes, quote_total, status, created_at"
    )
    .order("created_at", { ascending: false });
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    customerName: row.customer_name,
    company: row.company,
    phone: row.phone,
    itemName: row.item_name,
    units: row.units,
    unitPrice: row.unit_price !== null ? Number(row.unit_price) : null,
    fulfillmentType: row.fulfillment_type,
    eventDate: row.event_date,
    notes: row.notes,
    quoteTotal: row.quote_total !== null ? Number(row.quote_total) : null,
    status: row.status,
    createdAt: row.created_at,
  }));
}

export async function updateWholesaleStatus(id: string, status: string) {
  const { error } = await supabase.from("wholesale_orders").update({ status }).eq("id", id);
  if (error) throw error;
}

export type AdminJobApplication = {
  id: string;
  name: string;
  phone: string | null;
  email: string | null;
  positionTitle: string | null;
  status: string;
  createdAt: string;
};

export async function fetchAdminJobApplications(): Promise<AdminJobApplication[]> {
  const { data, error } = await supabase
    .from("job_applications")
    .select("id, name, phone, email, status, created_at, job_positions(title)")
    .order("created_at", { ascending: false });
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    name: row.name,
    phone: row.phone,
    email: row.email,
    positionTitle: (row.job_positions as unknown as { title: string } | null)?.title ?? null,
    status: row.status,
    createdAt: row.created_at,
  }));
}

export async function updateJobApplicationStatus(id: string, status: string) {
  const { error } = await supabase.from("job_applications").update({ status }).eq("id", id);
  if (error) throw error;
}

export type AdminReferral = {
  id: string;
  candidateName: string;
  candidatePhone: string | null;
  positionTitle: string | null;
  status: string;
  hireDate: string | null;
  bonusClaimDeadline: string | null;
  bonusClaimed: boolean;
  createdAt: string;
};

export async function fetchAdminReferrals(): Promise<AdminReferral[]> {
  const { data, error } = await supabase
    .from("employee_referrals")
    .select(
      "id, candidate_name, candidate_phone, status, hire_date, bonus_claim_deadline, bonus_claimed, created_at, job_positions(title)"
    )
    .order("created_at", { ascending: false });
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    candidateName: row.candidate_name,
    candidatePhone: row.candidate_phone,
    positionTitle: (row.job_positions as unknown as { title: string } | null)?.title ?? null,
    status: row.status,
    hireDate: row.hire_date,
    bonusClaimDeadline: row.bonus_claim_deadline,
    bonusClaimed: row.bonus_claimed,
    createdAt: row.created_at,
  }));
}

export async function updateReferralStatus(id: string, status: string) {
  const { error } = await supabase.from("employee_referrals").update({ status }).eq("id", id);
  if (error) throw error;
}

/**
 * Marking a referral "hired" starts the clock required by the client's spec:
 * the referral bonus becomes payable once the candidate reaches 3 months of
 * tenure, with a 7-day window afterward to claim it.
 */
export async function markReferralHired(id: string) {
  const hireDate = new Date();
  const claimDeadline = new Date(hireDate);
  claimDeadline.setMonth(claimDeadline.getMonth() + 3);
  claimDeadline.setDate(claimDeadline.getDate() + 7);

  const { error } = await supabase
    .from("employee_referrals")
    .update({
      status: "hired",
      hire_date: hireDate.toISOString().slice(0, 10),
      bonus_claim_deadline: claimDeadline.toISOString().slice(0, 10),
    })
    .eq("id", id);
  if (error) throw error;
}

export async function claimReferralBonus(id: string) {
  const { error } = await supabase.from("employee_referrals").update({ bonus_claimed: true }).eq("id", id);
  if (error) throw error;
}

export async function fetchAdminGoogleReviewsCount(): Promise<number> {
  const { count, error } = await supabase.from("google_reviews").select("id", { count: "exact", head: true });
  if (error) throw error;
  return count ?? 0;
}

export type AdminCustomer = {
  id: string;
  name: string;
  email: string | null;
  phone: string | null;
  memberCode: string | null;
  points: number;
  walletBalance: number;
  createdAt: string;
};

export async function fetchAdminCustomers(): Promise<AdminCustomer[]> {
  const { data, error } = await supabase
    .from("customers")
    .select("id, name, email, phone, member_code, points, wallet_balance, created_at")
    .order("created_at", { ascending: false });
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    name: row.name,
    email: row.email,
    phone: row.phone,
    memberCode: row.member_code,
    points: row.points,
    walletBalance: Number(row.wallet_balance),
    createdAt: row.created_at,
  }));
}

export type AdminCoupon = {
  id: string;
  code: string;
  title: string;
  description: string;
  discountLabel: string;
  discountPercent: number | null;
  validUntil: string | null;
  onlyOnline: boolean;
  active: boolean;
};

export async function fetchAdminCoupons(): Promise<AdminCoupon[]> {
  const { data, error } = await supabase
    .from("coupons")
    .select("id, code, title, description, discount_label, discount_percent, valid_until, only_online, active")
    .order("created_at", { ascending: false });
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    code: row.code,
    title: row.title,
    description: row.description ?? "",
    discountLabel: row.discount_label,
    discountPercent: row.discount_percent,
    validUntil: row.valid_until,
    onlyOnline: row.only_online,
    active: row.active,
  }));
}

export async function createCoupon(params: {
  code: string;
  title: string;
  description: string;
  discountLabel: string;
  discountPercent: number | null;
  validUntil: string | null;
  onlyOnline: boolean;
}) {
  const { error } = await supabase.from("coupons").insert({
    code: params.code.toUpperCase(),
    title: params.title,
    description: params.description,
    discount_label: params.discountLabel,
    discount_percent: params.discountPercent,
    valid_until: params.validUntil,
    only_online: params.onlyOnline,
  });
  if (error) throw error;
}

export async function toggleCouponActive(id: string, active: boolean) {
  const { error } = await supabase.from("coupons").update({ active }).eq("id", id);
  if (error) throw error;
}

export async function updateProductPricing(
  productId: string,
  patch: { priceMedium: number | null; priceLarge: number | null; active: boolean }
) {
  const { error } = await supabase
    .from("products")
    .update({ price_medium: patch.priceMedium, price_large: patch.priceLarge, active: patch.active })
    .eq("id", productId);
  if (error) throw error;
}

export type AdminProduct = {
  id: string;
  name: string;
  categoryName: string;
  priceMedium: number | null;
  priceLarge: number | null;
  active: boolean;
};

export async function fetchAdminProducts(): Promise<AdminProduct[]> {
  const { data, error } = await supabase
    .from("products")
    .select("id, name, price_medium, price_large, active, categories(name)")
    .order("name");
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    name: row.name,
    categoryName: (row.categories as unknown as { name: string } | null)?.name ?? "",
    priceMedium: row.price_medium !== null ? Number(row.price_medium) : null,
    priceLarge: row.price_large !== null ? Number(row.price_large) : null,
    active: row.active,
  }));
}

// =========================================================
// Punto de venta (POS) — ventas reales de empleados
// =========================================================

export type ActiveOrder = {
  id: string;
  status: string;
  createdAt: string;
  items: Array<{ name: string; qty: number }>;
};

export async function fetchActiveOrders(): Promise<ActiveOrder[]> {
  const { data, error } = await supabase
    .from("orders")
    .select("id, status, created_at, order_items(name, qty)")
    .in("status", ["pending", "preparing", "ready"])
    .order("created_at", { ascending: false })
    .limit(12);
  if (error) throw error;
  return data.map((row) => ({
    id: row.id,
    status: row.status,
    createdAt: row.created_at,
    items: (row.order_items as Array<{ name: string; qty: number }>) ?? [],
  }));
}

export async function submitPosSale(params: {
  paymentMethod: "card" | "cash";
  subtotal: number;
  discount?: number;
  total: number;
  items: Array<{ name: string; price: number; qty: number }>;
}) {
  const orderId = crypto.randomUUID();
  const { error: orderError } = await supabase.from("orders").insert({
    id: orderId,
    fulfillment_type: "pickup",
    address: null,
    payment_method: params.paymentMethod,
    subtotal: params.subtotal,
    shipping_cost: 0,
    discount: params.discount ?? 0,
    coupon_code: null,
    total: params.total,
    status: "preparing",
    payment_status: "paid",
  });
  if (orderError) throw orderError;

  const { error: itemsError } = await supabase.from("order_items").insert(
    params.items.map((item) => ({ order_id: orderId, name: item.name, price: item.price, qty: item.qty }))
  );
  if (itemsError) throw itemsError;

  return orderId;
}

// =========================================================
// Fidelización: gift cards y suscripciones
// =========================================================

export async function sendGiftCard(senderContact: string, recipientContact: string, amount: number): Promise<LoyaltySnapshot> {
  const { data, error } = await supabase.rpc("send_gift_card", {
    p_sender_contact: senderContact,
    p_recipient_contact: recipientContact,
    p_amount: amount,
  });
  if (error) {
    const map: Record<string, string> = {
      monto_invalido: "Ingresa un monto válido.",
      cuenta_no_encontrada: "No encontramos tu cuenta.",
      destinatario_no_encontrado: "No encontramos una cuenta con ese correo o teléfono.",
      saldo_insuficiente: "No tienes saldo suficiente en tu monedero.",
    };
    const key = Object.keys(map).find((k) => error.message.includes(k));
    throw new Error(key ? map[key] : error.message);
  }
  return mapLoyaltySnapshot(data);
}

export async function activateSubscription(contact: string, planSlug: string): Promise<LoyaltySnapshot> {
  const { data, error } = await supabase.rpc("activate_subscription", { p_contact: contact, p_plan_slug: planSlug });
  if (error) {
    if (error.message.includes("cuenta_no_encontrada")) {
      throw new Error("Necesitas una cuenta de fidelización para activar un plan.");
    }
    throw error;
  }
  return mapLoyaltySnapshot(data);
}

// =========================================================
// Reclutamiento: CV en Supabase Storage
// =========================================================

export async function uploadJobApplicationCv(file: File): Promise<string> {
  const path = `${crypto.randomUUID()}-${file.name}`;
  const { error } = await supabase.storage.from("job-application-cvs").upload(path, file);
  if (error) throw error;
  return path;
}

export async function getJobApplicationCvUrl(path: string): Promise<string> {
  const { data, error } = await supabase.storage.from("job-application-cvs").createSignedUrl(path, 120);
  if (error) throw error;
  return data.signedUrl;
}
</file>

<file path="src/lib/supabase.ts">
import { createClient } from "@supabase/supabase-js";

export const supabase = createClient(
  import.meta.env.PUBLIC_SUPABASE_URL,
  import.meta.env.PUBLIC_SUPABASE_ANON_KEY
);
</file>

<file path="src/lib/utils.ts">
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

export function formatMXN(amount: number) {
  return amount.toLocaleString("es-MX", {
    style: "currency",
    currency: "MXN",
    maximumFractionDigits: 0,
  });
}
</file>

<file path="src/pages/carrito.astro">
---
import Layout from "@/layouts/Layout.astro";
import CartCheckout from "@/components/islands/CartCheckout";
---

<Layout title="Carrito" description="Revisa tu pedido y elige entrega o recolección en tienda.">
  <header class="mb-10">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      🛒 Tu carrito
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Finaliza tu pedido</h1>
  </header>

  <CartCheckout client:load />
</Layout>
</file>

<file path="src/pages/empleados.astro">
---
import Layout from "@/layouts/Layout.astro";
import EmployeePortal from "@/components/islands/EmployeePortal";
---

<Layout title="Portal de empleados" description="Capacitación, manuales, recomendación de candidatos y bolsa de trabajo.">
  <header class="mb-8">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      🧑‍🍳 Portal interno
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Portal de empleados</h1>
    <p class="mt-3 max-w-2xl text-sm text-carrot-50/70 sm:text-base">
      Capacitación, manuales de puesto, recomendación de candidatos y reclutamiento, compatible con tablets.
    </p>
  </header>

  <EmployeePortal client:load />
</Layout>
</file>

<file path="src/pages/encuestas.astro">
---
import Layout from "@/layouts/Layout.astro";
import SurveyForm from "@/components/islands/SurveyForm";
---

<Layout title="Encuestas" description="Encuesta de satisfacción, evaluación de sucursal y votación de cajero favorito.">
  <header class="mb-10 text-center">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      📝 Encuesta de satisfacción
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Cuéntanos cómo estuvo tu visita</h1>
    <p class="mx-auto mt-3 max-w-xl text-sm text-carrot-50/70 sm:text-base">
      Tu opinión nos ayuda a mejorar y a reconocer al equipo que te atendió.
    </p>
  </header>

  <SurveyForm client:load />
</Layout>
</file>

<file path="src/pages/fidelizacion.astro">
---
import Layout from "@/layouts/Layout.astro";
import LoyaltyDashboard from "@/components/islands/LoyaltyDashboard";
---

<Layout title="Fidelización" description="Programa de puntos, monedero electrónico, gift cards y código QR.">
  <header class="mb-10">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      💳 Fidelización
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Tu monedero Don Zanahorio</h1>
    <p class="mt-3 max-w-2xl text-sm text-carrot-50/70 sm:text-base">
      Acumula mínimo 10% de cada compra en puntos, guarda saldo en tu monedero digital y valida todo con tu código
      QR personal.
    </p>
  </header>

  <LoyaltyDashboard client:load />
</Layout>
</file>

<file path="src/pages/mayoreo.astro">
---
import Layout from "@/layouts/Layout.astro";
import MayoreoForm from "@/components/islands/MayoreoForm";
---

<Layout title="Pedidos de mayoreo" description="Cotiza pedidos por volumen para eventos, oficinas y negocios.">
  <header class="mb-10">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      📦 Mayoreo
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Pedidos por volumen</h1>
    <p class="mt-3 max-w-2xl text-sm text-carrot-50/70 sm:text-base">
      Ideal para oficinas, eventos y catering. Elige el producto y la cantidad para ver tu cotización automática al
      instante.
    </p>
  </header>

  <MayoreoForm client:load />
</Layout>
</file>

<file path="src/pages/menu.astro">
---
import Layout from "@/layouts/Layout.astro";
import MenuCatalog from "@/components/islands/MenuCatalog";
---

<Layout title="Menú digital" description="Explora el menú completo de Don Zanahorio y arma tu pedido.">
  <header class="mb-10">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      🍽️ Menú digital
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Elige, agrega y ordena</h1>
    <p class="mt-3 max-w-2xl text-sm text-carrot-50/70 sm:text-base">
      Todo nuestro catálogo, siempre actualizado. Agrega tus platillos favoritos al carrito y finaliza tu pedido
      para recoger en tienda o recibirlo a domicilio.
    </p>
  </header>

  <MenuCatalog client:load />
</Layout>
</file>

<file path="src/pages/pos.astro">
---
import Layout from "@/layouts/Layout.astro";
import POSIsland from "@/components/islands/POSIsland";
import { INVENTORY, isLowStock } from "@/data/inventory";
---

<Layout title="Punto de venta" description="Sistema POS táctil para el equipo de Don Zanahorio.">
  <header class="mb-8">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      🧑‍🍳 Portal interno
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Punto de venta</h1>
    <p class="mt-3 max-w-2xl text-sm text-carrot-50/70 sm:text-base">
      Interfaz optimizada para tablets y pantallas táctiles en caja. Requiere inicio de sesión de empleado.
    </p>
  </header>

  <POSIsland client:load />

  <section class="glass mt-10 rounded-2xl p-5">
    <h2 class="text-sm font-semibold uppercase tracking-wider text-carrot-300">Inventario en tiempo real</h2>
    <div class="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
      {INVENTORY.map((item) => (
        <div class="glass glass-card flex items-center justify-between rounded-xl p-4">
          <div>
            <p class="text-sm font-medium">{item.ingredient}</p>
            <p class="text-xs text-carrot-50/50">
              {item.stock} {item.unit} disponibles
            </p>
          </div>
          {isLowStock(item) && (
            <span class="rounded-full bg-red-500/20 px-2 py-1 text-[10px] font-semibold text-red-300">
              Stock bajo
            </span>
          )}
        </div>
      ))}
    </div>
  </section>
</Layout>
</file>

<file path="src/pages/promociones.astro">
---
import Layout from "@/layouts/Layout.astro";
import PromotionsBoard from "@/components/islands/PromotionsBoard";
import GoogleReviewLink from "@/components/islands/GoogleReviewLink";
import { SITE } from "@/data/site";
---

<Layout title="Promociones" description="Cupones automáticos, descuentos exclusivos web y productos destacados.">
  <header class="mb-10">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      🏷️ Promociones
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Cupones y descuentos vigentes</h1>
    <p class="mt-3 max-w-2xl text-sm text-carrot-50/70 sm:text-base">
      Aplica estos cupones directamente en tu carrito. Los descuentos web son exclusivos para pedidos en línea.
    </p>
  </header>

  <PromotionsBoard client:load />

  <section class="glass-strong mt-16 rounded-[2rem] px-6 py-12 text-center sm:px-14">
    <span class="text-4xl">⭐</span>
    <h2 class="mt-4 text-2xl font-bold sm:text-3xl">Déjanos tu reseña en Google</h2>
    <p class="mx-auto mt-3 max-w-xl text-sm text-carrot-50/70">
      Comparte tu experiencia en {SITE.city} y recibe un cupón de 20% para tu próxima visita. Registramos tu reseña
      para hacerte llegar el beneficio.
    </p>
    <GoogleReviewLink client:load className="mt-6" />
  </section>
</Layout>
</file>

<file path="src/pages/suscripciones.astro">
---
import Layout from "@/layouts/Layout.astro";
import PlansList from "@/components/islands/PlansList";
---

<Layout title="Suscripciones" description="Membresías mensuales con precio preferencial, validadas por QR o teléfono.">
  <header class="mb-10">
    <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
      🔁 Membresías
    </span>
    <h1 class="mt-4 text-3xl font-bold sm:text-5xl">Suscripciones mensuales</h1>
    <p class="mt-3 max-w-2xl text-sm text-carrot-50/70 sm:text-base">
      Precio preferencial todos los días. Se valida con tu código QR o número telefónico, tanto en sucursal como a
      domicilio.
    </p>
  </header>

  <PlansList client:load />

  <p class="mt-8 text-center text-xs text-carrot-50/50">
    Todos los planes se pueden cancelar en cualquier momento desde tu cuenta de fidelización.
  </p>
</Layout>
</file>

<file path="src/stores/cart.ts">
import { computed } from "nanostores";
import { persistentMap } from "@nanostores/persistent";

export type CartLine = {
  id: string;
  name: string;
  price: number;
  qty: number;
  emoji: string;
};

type CartState = Record<string, string>;

export const $cartRaw = persistentMap<CartState>("dz-cart:", {});

function readLine(key: string): CartLine | null {
  const raw = $cartRaw.get()[key];
  if (!raw) return null;
  try {
    return JSON.parse(raw) as CartLine;
  } catch {
    return null;
  }
}

export function getCartLines(): CartLine[] {
  return Object.keys($cartRaw.get())
    .map(readLine)
    .filter((l): l is CartLine => l !== null)
    .sort((a, b) => a.name.localeCompare(b.name));
}

export const $cartLines = computed($cartRaw, () => getCartLines());

export const $cartCount = computed($cartLines, (lines) =>
  lines.reduce((sum, l) => sum + l.qty, 0)
);

export const $cartTotal = computed($cartLines, (lines) =>
  lines.reduce((sum, l) => sum + l.qty * l.price, 0)
);

export function addToCart(item: Omit<CartLine, "qty">, qty = 1) {
  const existing = readLine(item.id);
  const nextQty = (existing?.qty ?? 0) + qty;
  $cartRaw.setKey(item.id, JSON.stringify({ ...item, qty: nextQty }));
}

export function setQty(id: string, qty: number) {
  const existing = readLine(id);
  if (!existing) return;
  if (qty <= 0) {
    $cartRaw.setKey(id, undefined as unknown as string);
    return;
  }
  $cartRaw.setKey(id, JSON.stringify({ ...existing, qty }));
}

export function removeFromCart(id: string) {
  $cartRaw.setKey(id, undefined as unknown as string);
}

export function clearCart() {
  for (const key of Object.keys($cartRaw.get())) {
    $cartRaw.setKey(key, undefined as unknown as string);
  }
}
</file>

<file path="src/stores/loyalty.ts">
import { persistentAtom } from "@nanostores/persistent";
import { atom } from "nanostores";
import {
  registerLoyaltyCustomer,
  lookupLoyaltyCustomer,
  activateSubscription,
  type LoyaltySnapshot,
} from "@/lib/queries";

export type LoyaltyProfile = LoyaltySnapshot;

/** Only remembers *which* contact is signed in on this device — the balances always come from Supabase. */
export const $loyaltyContact = persistentAtom<string | undefined>("dz-loyalty-contact", undefined);
export const $loyaltyProfile = atom<LoyaltyProfile | null>(null);
export const $loyaltyLoading = atom(false);

export async function restoreLoyaltySession() {
  const contact = $loyaltyContact.get();
  if (!contact) return;
  $loyaltyLoading.set(true);
  try {
    const snapshot = await lookupLoyaltyCustomer(contact);
    $loyaltyProfile.set(snapshot);
    if (!snapshot) $loyaltyContact.set(undefined);
  } finally {
    $loyaltyLoading.set(false);
  }
}

export async function registerLoyalty(name: string, contact: string, referralCode?: string) {
  const snapshot = await registerLoyaltyCustomer(name, contact, referralCode);
  $loyaltyContact.set(contact);
  $loyaltyProfile.set(snapshot);
  return snapshot;
}

export async function loginLoyalty(contact: string) {
  const snapshot = await lookupLoyaltyCustomer(contact);
  if (!snapshot) {
    throw new Error("No encontramos una cuenta con ese correo o teléfono.");
  }
  $loyaltyContact.set(contact);
  $loyaltyProfile.set(snapshot);
  return snapshot;
}

export function logoutLoyalty() {
  $loyaltyContact.set(undefined);
  $loyaltyProfile.set(null);
}

export async function activatePlan(planSlug: string) {
  const contact = $loyaltyContact.get();
  if (!contact) throw new Error("Necesitas una cuenta de fidelización para activar un plan.");
  const snapshot = await activateSubscription(contact, planSlug);
  $loyaltyProfile.set(snapshot);
  return snapshot;
}
</file>

<file path="src/styles/global.css">
@import "tailwindcss";
@import "@fontsource-variable/geist";

@theme {
  --font-sans: "Geist Variable", ui-sans-serif, system-ui, sans-serif;

  /* Zanahoria — naranja cálido de marca */
  --color-carrot-50: #fff4ec;
  --color-carrot-100: #ffe4cf;
  --color-carrot-200: #ffc79b;
  --color-carrot-300: #ffa25e;
  --color-carrot-400: #ff8a3d;
  --color-carrot-500: #ff7a1a;
  --color-carrot-600: #f2620a;
  --color-carrot-700: #c94b08;
  --color-carrot-800: #9e3b0d;
  --color-carrot-900: #7f320f;

  /* Hoja de zanahoria — verde fresco */
  --color-leaf-50: #eefdf3;
  --color-leaf-100: #d5f9e0;
  --color-leaf-200: #adf1c4;
  --color-leaf-300: #74e29f;
  --color-leaf-400: #3fca7a;
  --color-leaf-500: #1fae5f;
  --color-leaf-600: #148c4c;
  --color-leaf-700: #126f3f;
  --color-leaf-800: #125835;
  --color-leaf-900: #10482d;

  /* Fondo "tierra" profundo para el efecto de vidrio */
  --color-ink-950: #0f0b09;
  --color-ink-900: #17110d;
  --color-ink-800: #241a13;
  --color-ink-700: #33241a;

  --shadow-glass: 0 8px 32px 0 rgba(15, 11, 9, 0.45);
  --shadow-glass-lg: 0 24px 64px -12px rgba(15, 11, 9, 0.6);
}

@layer base {
  html {
    color-scheme: dark;
    scroll-behavior: smooth;
  }

  body {
    background-color: var(--color-ink-950);
    color: var(--color-carrot-50);
    font-family: var(--font-sans);
    min-height: 100svh;
  }

  ::selection {
    background-color: var(--color-carrot-500);
    color: var(--color-ink-950);
  }
}

@layer components {
  /* Fondo ambiental con manchas de color desenfocadas, fijo detrás de todo */
  .ambient-bg {
    position: fixed;
    inset: 0;
    z-index: -10;
    overflow: hidden;
    background:
      radial-gradient(ellipse 80% 60% at 15% 0%, rgba(255, 122, 26, 0.35), transparent 60%),
      radial-gradient(ellipse 70% 60% at 100% 20%, rgba(31, 174, 95, 0.28), transparent 55%),
      radial-gradient(ellipse 60% 50% at 50% 100%, rgba(255, 138, 61, 0.2), transparent 60%),
      var(--color-ink-950);
  }

  .ambient-blob {
    position: absolute;
    border-radius: 999px;
    filter: blur(90px);
    opacity: 0.55;
    animation: float 18s ease-in-out infinite;
  }

  .glass {
    background: rgba(255, 244, 236, 0.06);
    backdrop-filter: blur(20px) saturate(140%);
    -webkit-backdrop-filter: blur(20px) saturate(140%);
    border: 1px solid rgba(255, 244, 236, 0.14);
    box-shadow: var(--shadow-glass);
  }

  .glass-strong {
    background: rgba(255, 244, 236, 0.1);
    backdrop-filter: blur(28px) saturate(150%);
    -webkit-backdrop-filter: blur(28px) saturate(150%);
    border: 1px solid rgba(255, 244, 236, 0.18);
    box-shadow: var(--shadow-glass-lg);
  }

  .glass-card {
    transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
  }

  .glass-card:hover {
    transform: translateY(-3px);
    border-color: rgba(255, 138, 61, 0.4);
  }

  .text-gradient {
    background: linear-gradient(100deg, var(--color-carrot-300), var(--color-leaf-300));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }
}

@keyframes float {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50% { transform: translate(3%, 4%) scale(1.08); }
}

@media (prefers-reduced-motion: reduce) {
  .ambient-blob {
    animation: none;
  }
}
</file>

<file path="src/env.d.ts">
/// <reference types="astro/client" />

interface ImportMetaEnv {
  readonly PUBLIC_SUPABASE_URL: string;
  readonly PUBLIC_SUPABASE_ANON_KEY: string;
  /** Server-only: never referenced from client components. */
  readonly STRIPE_SECRET_KEY: string;
  /** Server-only: shared secret so only our own server can confirm a Stripe payment via mark_order_paid(). */
  readonly INTERNAL_RPC_SECRET: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
</file>

<file path="pnpm-workspace.yaml">
allowBuilds:
  esbuild: true
  sharp: true
minimumReleaseAgeExclude:
  - astro@7.1.4
</file>

<file path="README.md">
# Astro Starter Kit: Minimal

```sh
pnpm create astro@latest -- --template minimal
```

> 🧑‍🚀 **Seasoned astronaut?** Delete this file. Have fun!

## 🚀 Project Structure

Inside of your Astro project, you'll see the following folders and files:

```text
/
├── public/
├── src/
│   └── pages/
│       └── index.astro
└── package.json
```

Astro looks for `.astro` or `.md` files in the `src/pages/` directory. Each page is exposed as a route based on its file name.

There's nothing special about `src/components/`, but that's where we like to put any Astro/React/Vue/Svelte/Preact components.

Any static assets, like images, can be placed in the `public/` directory.

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command                   | Action                                           |
| :------------------------ | :----------------------------------------------- |
| `pnpm install`             | Installs dependencies                            |
| `pnpm dev`             | Starts local dev server at `localhost:4321`      |
| `pnpm build`           | Build your production site to `./dist/`          |
| `pnpm preview`         | Preview your build locally, before deploying     |
| `pnpm astro ...`       | Run CLI commands like `astro add`, `astro check` |
| `pnpm astro -- --help` | Get help using the Astro CLI                     |

## 👀 Want to learn more?

Feel free to check [our documentation](https://docs.astro.build) or jump into our [Discord server](https://astro.build/chat).
</file>

<file path="src/pages/index.astro">
---
import Layout from "@/layouts/Layout.astro";
import CategoriesGrid from "@/components/islands/CategoriesGrid";
import { SITE, whatsappLink } from "@/data/site";
import { TESTIMONIALS } from "@/data/team";

const modules = [
  {
    emoji: "🛒",
    title: "Menú y pedidos en línea",
    description: "Catálogo digital completo, carrito, entrega a domicilio o para recoger, y pedidos de mayoreo.",
    href: "/menu",
    cta: "Ver menú",
  },
  {
    emoji: "💳",
    title: "Fidelización y monedero",
    description: "Puntos, monedero electrónico, gift cards y validación con código QR.",
    href: "/fidelizacion",
    cta: "Conocer beneficios",
  },
  {
    emoji: "🏷️",
    title: "Promociones inteligentes",
    description: "Cupones automáticos, descuentos exclusivos web y productos destacados.",
    href: "/promociones",
    cta: "Ver promociones",
  },
  {
    emoji: "🧑‍🍳",
    title: "Portal interno del equipo",
    description: "POS táctil, control de comandas, inventario y capacitación del personal.",
    href: "/pos",
    cta: "Ver portal interno",
  },
];

const steps = [
  { n: "1", title: "Elige tus platillos", text: "Explora el menú digital por categorías y arma tu pedido." },
  { n: "2", title: "Recoge o recibe a domicilio", text: "Calculamos el costo de envío automáticamente según tu zona." },
  { n: "3", title: "Gana puntos y beneficios", text: "Cada compra suma a tu monedero y desbloquea recompensas." },
];
---

<Layout title="Inicio" description="Menú digital, pedidos en línea, fidelización y más en Don Zanahorio, Ciudad Victoria.">
  <section class="glass-strong relative overflow-hidden rounded-[2rem] px-6 py-14 sm:px-14 sm:py-20">
    <div class="relative z-10 max-w-2xl">
      <span class="glass inline-flex items-center gap-2 rounded-full px-3 py-1 text-xs font-medium text-carrot-200">
        📍 {SITE.city}, {SITE.state}
      </span>
      <h1 class="mt-5 text-4xl font-bold leading-tight tracking-tight sm:text-6xl">
        Sabor <span class="text-gradient">casero</span> que se nota en cada bocado
      </h1>
      <p class="mt-5 text-base text-carrot-50/75 sm:text-lg">
        {SITE.tagline}. Pide en línea, recoge en tienda o recibe a domicilio — y acumula
        recompensas en cada visita.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a
          href="/menu"
          class="rounded-full bg-carrot-500 px-6 py-3 text-sm font-semibold text-ink-950 shadow-glass-lg transition hover:bg-carrot-400"
        >
          Ver el menú
        </a>
        <a
          href={whatsappLink("¡Hola! Quiero hacer un pedido en Don Zanahorio 🥕")}
          target="_blank"
          rel="noopener noreferrer"
          class="glass glass-card rounded-full px-6 py-3 text-sm font-semibold text-carrot-50"
        >
          Pedir por WhatsApp
        </a>
      </div>
      <div class="mt-10 flex flex-wrap gap-2 text-xs text-carrot-50/70">
        <span class="glass rounded-full px-3 py-1.5">🚚 Envío a domicilio</span>
        <span class="glass rounded-full px-3 py-1.5">🏬 Recoge en tienda</span>
        <span class="glass rounded-full px-3 py-1.5">⭐ Programa de puntos 10%+</span>
        <span class="glass rounded-full px-3 py-1.5">📦 Pedidos de mayoreo</span>
      </div>
    </div>

    <div class="pointer-events-none absolute -right-10 -top-10 hidden text-[16rem] leading-none opacity-20 sm:block">
      🥕
    </div>
  </section>

  <section class="mt-16">
    <div class="flex items-end justify-between gap-4">
      <h2 class="text-2xl font-bold sm:text-3xl">Todo lo que necesita tu restaurante</h2>
    </div>
    <p class="mt-2 max-w-2xl text-sm text-carrot-50/70">
      Una plataforma integrada: portal público, fidelización, promociones y sistema interno para tu equipo.
    </p>
    <div class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
      {modules.map((m) => (
        <a href={m.href} class="glass glass-card flex flex-col rounded-2xl p-6">
          <span class="text-3xl">{m.emoji}</span>
          <h3 class="mt-4 text-lg font-semibold">{m.title}</h3>
          <p class="mt-2 flex-1 text-sm text-carrot-50/70">{m.description}</p>
          <span class="mt-4 text-sm font-semibold text-carrot-300">{m.cta} →</span>
        </a>
      ))}
    </div>
  </section>

  <section class="mt-20">
    <div class="flex items-end justify-between gap-4">
      <h2 class="text-2xl font-bold sm:text-3xl">Explora nuestro menú por categoría</h2>
      <a href="/menu" class="text-sm font-semibold text-carrot-300 hover:text-carrot-200">Ver menú completo →</a>
    </div>
    <CategoriesGrid client:visible />
  </section>

  <section class="mt-20 grid gap-5 lg:grid-cols-3">
    {steps.map((s) => (
      <div class="glass rounded-2xl p-6">
        <span class="grid h-10 w-10 place-items-center rounded-full bg-carrot-500/20 font-bold text-carrot-300">
          {s.n}
        </span>
        <h3 class="mt-4 font-semibold">{s.title}</h3>
        <p class="mt-1 text-sm text-carrot-50/70">{s.text}</p>
      </div>
    ))}
  </section>

  <section class="mt-20">
    <h2 class="text-2xl font-bold sm:text-3xl">Lo que dicen nuestros clientes</h2>
    <div class="mt-8 grid gap-5 sm:grid-cols-3">
      {TESTIMONIALS.map((t) => (
        <div class="glass rounded-2xl p-6">
          <div class="text-carrot-300">{"★".repeat(t.rating)}{"☆".repeat(5 - t.rating)}</div>
          <p class="mt-3 text-sm text-carrot-50/80">“{t.quote}”</p>
          <p class="mt-4 text-xs font-semibold text-carrot-50/60">{t.name}</p>
        </div>
      ))}
    </div>
  </section>

  <section class="glass-strong mt-20 rounded-[2rem] px-6 py-12 text-center sm:px-14">
    <h2 class="text-2xl font-bold sm:text-3xl">¿Listo para probar Don Zanahorio?</h2>
    <p class="mx-auto mt-3 max-w-xl text-sm text-carrot-50/70">
      Haz tu pedido en línea o visítanos en {SITE.address}, {SITE.city}, {SITE.state}.
    </p>
    <div class="mt-7 flex flex-wrap justify-center gap-3">
      <a href="/menu" class="rounded-full bg-carrot-500 px-6 py-3 text-sm font-semibold text-ink-950 shadow-glass-lg hover:bg-carrot-400">
        Ordenar ahora
      </a>
      <a href="/fidelizacion" class="glass glass-card rounded-full px-6 py-3 text-sm font-semibold text-carrot-50">
        Únete a fidelización
      </a>
    </div>
  </section>
</Layout>
</file>

<file path="AGENTS.md">
## 🎯 Alcance del Proyecto y Módulos Clave

Al generar código o proponer soluciones, mantén presente la arquitectura funcional de los siguientes módulos:

1. **E-Commerce & Portal Público (Astro + React):**
   - Menú digital responsivo y catálogo de productos.
   - Carrito de compras, pedidos para llevar/domicilio y sección de **pedidos de mayoreo**.
   - Encuestas de satisfacción dinámicas post-compra.

2. **Portal de Empleados & Sistema POS (React / Islas):**
   - Interfaz ágil para punto de venta (optimizado para tablets/pantallas táctiles).
   - Módulo de capacitación interna para el personal.
   - Panel de control de comandas e inventario en tiempo real.

3. **Fidelización y Monedero Electrónico:**
   - Sistema de acumulación de puntos y monedero digital por cliente.
   - Validación y lectura rápida mediante **código QR**.

4. **Motor de Promociones y Reglas de Negocio:**
   - Creación y cálculo dinámico de descuentos, cupones y reglas acumulables.

---

## 🛠️ Stack Técnico Oficial

- **Frontend:** Astro 6.4.6 + React 19 (Islas de interactividad)
- **Styling:** Tailwind CSS 4.3 + shadcn/ui
- **Fuentes:** Geist (vía `@fontsource-variable/geist`)
- **Gestor de Paquetes:** `pnpm`
- **Base de Datos & ORM:** PostgreSQL + Prisma ORM (en la nube)
- **Infraestructura & Deploy:** Vercel (Analytics + Speed Insights)
- **Entorno:** Node.js >=22.12.0
</file>

<file path="tsconfig.json">
{
  "extends": "astro/tsconfigs/strict",
  "include": [
    ".astro/types.d.ts",
    "**/*"
  ],
  "exclude": [
    "dist"
  ],
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
</file>

<file path=".gitignore">
# build output
dist/
# generated types
.astro/
.claude/
.codegraph/
.vscode/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env.local
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
.vercel

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

#directories
test-results/
e2e/
</file>

<file path="astro.config.mjs">
// @ts-check
import { fileURLToPath } from 'node:url';
import { defineConfig } from 'astro/config';

import react from '@astrojs/react';
import tailwindcss from '@tailwindcss/vite';

import vercel from '@astrojs/vercel';

// https://astro.build/config
export default defineConfig({
  output: 'server',

  integrations: [react()],

  vite: {
    plugins: [tailwindcss()],
    resolve: {
      alias: {
        '@': fileURLToPath(new URL('./src', import.meta.url))
      }
    }
  },

  adapter: vercel()
});
</file>

<file path="package.json">
{
  "name": "demo-don-zanahorio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:safe": "playwright test --grep-invert @writes-data",
    "test:e2e:report": "playwright show-report"
  },
  "dependencies": {
    "@astrojs/react": "^6.0.1",
    "@astrojs/vercel": "^11.0.3",
    "@fontsource-variable/geist": "^5.2.6",
    "@nanostores/persistent": "^0.10.2",
    "@nanostores/react": "^0.8.4",
    "@supabase/supabase-js": "^2.110.8",
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "astro": "^7.1.4",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.469.0",
    "nanostores": "^0.11.4",
    "qrcode": "^1.5.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "stripe": "^18.0.0",
    "tailwind-merge": "^2.6.0",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.62.0",
    "@types/qrcode": "^1.5.5",
    "typescript": "^5.9.3"
  }
}
</file>

</files>
